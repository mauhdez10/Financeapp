# ISSUES_LEDGER.md — Golden Anchor Finance

> Brief, one-line-per-issue tracker of known problems + their history. The review handler
> (`finance-review-mode`, §4c) checks this **backward**: a new report that matches an existing
> entry is a **recurrence** — root-cause the old fix, don't re-paste it. Keep entries terse;
> the "why" of any shipped fix lives in `CHANGELOG.md`. Newest on top.
>
> Status: 🔴 open · 🟡 owner-decision · 🟢 fixed (kept briefly for recurrence-matching, then pruned) · 🔁 recurrence

## Open

| ID | Area | Status | One-line | Notes / owner question |
|---|---|---|---|---|
| ISS-60 | save-path / money / scale | ⛔ | **`monthlyRows` (`finance.js:118`) computes `cardMins = Σ (+cd.min\|\|0)` (raw min) and writes `spending = bills + cardMins` into the persisted `client_monthly_summary` row** — canonical debt service is `sumMin` = Σ `effectiveMin` (§3), so the stored `spending` series diverges for the same four cases as ISS-59 (unset min, paid-off stale min, sub-$25 floor, min>balance). Save-path sibling of the display-only ISS-59. | **⛔attended** — persisted cms summary derivation over snapshot data + needs a row backfill, same disposition as ISS-18 (`monthlyRows.net_worth`). NOT autonomous. **Recommended fix:** `cardMins = cards.reduce((a,cd)=>a+effectiveMin(cd),0)` — bundle with the ISS-18 backfill (same fn/row/pass). Queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES, attended). Found tracing the ISS-59 raw-`.min` scan into the save path. |
| ISS-53 | save-path / scale / dashboard | ⛔ | **`clientSummary.assets` (`finance.js:107-110`) omits `marketInvestments`** from the dashboard Asset-Sunburst breakdown — it spreads `accounts` (bucketed by `ACCT_META`) + `customAssets` (bucket "property") but **not** MI holdings. Yet the SAME function's `net_worth` (`:98`) uses canonical `totalA(c)` which **includes** MI → on the advisor dashboard the net-worth KPI counts market investments but the asset-allocation sunburst doesn't show them (an MI-heavy client's allocation chart understates invested assets and won't reconcile to net worth). Same omission class as ISS-47/50/52, but here it feeds the **`client_summary` row** (scale data layer save path) → understated/missing only in the persisted summary, fixed on a re-save. | **⛔attended** — writes `client_summary` via the save path + needs a backfill of existing rows (consistent with ISS-18 `monthlyRows` net_worth ⛔). **Recommended fix:** add an `invest`-bucket spread of `marketInvestments` (`{bucket:"invest",name:ticker||name,val:value}`) to the `assets` array — purely additive, mirrors the existing `customAssets` spread. NOT an autonomous push (save-path + backfill). Found in the item-1 `utils/aiExport.js`/`finance.js` correctness scan (2026-06-26). |
| ISS-51 | save-path / money / import | ⛔ | **`smartMerge` (`utils/import.js:222`) drops `marketInvestments` on duplicate-client merge.** Its array-union list is `["incomeStreams","bills","cards","accounts","loans","customAssets"]` — `marketInvestments` is **absent**, so `merged={...existing}` keeps the existing blob's MI and the **incoming** client's MI holdings are never unioned in. `marketInvestments` IS canonical (`mk`/`mig` seed it, `totalA` includes it — same omission class as ISS-47/50, but here it's **persisted**, not display). Both call sites feed the live save path: backup-restore merge mode `gaSaveClient(…,smartMerge(mig(full),bc))` (`App.jsx:715`) and import merge `smartMerge(dup,bc)` (`App.jsx:718`). Backup blobs round-trip MI (`expBackup` writes raw `clients`; `validateBackup` only checks the envelope), so incoming genuinely carries it. Restore/import a dup in merge mode → MI silently lost → understated assets/net worth. | **⛔attended** — writes via `gaSaveClient` (live save path), can't verify the end-to-end restore headlessly (no backup fixtures; prod-mutation classifier blocks it) → NOT an autonomous push; consistent with ISS-16 (`importMultiple`) ⛔. node harness `scratchpad/iss51.mjs` (replicates smartMerge verbatim): current drops $60k/2 holdings→0; one-key fix preserves both. **Recommended fix:** add `"marketInvestments"` to `arrKeys` (same union-by-id as the other five; purely additive). Queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES, attended). Found in the item-1 `utils/import.js` correctness scan. |
| ISS-49 | report / money / save-path | ⛔ | **Premise INVERTED — harness-proven 2026-06-26 (write-path traced as the ISS-49 note asked).** `properties[]` has **no user-edit write path** anywhere (grep across `src`: the only writes are `mk()`→`[]` and `mig()`). So `getProperties(c)` (`finance.js:59` = `c.properties?.length ? properties : customAssets`) prefers a **phantom alias** that `mig` freezes to a one-time snapshot of `customAssets` and thereafter preserves verbatim (`properties:Array.isArray(c.properties)?c.properties:cleanProps`; `upClient` runs `mig` before every save, so the stale value persists). `CustomAssetsSection.save` (`clientSections.jsx:27`) writes `customAssets` only. Result: for any blob that ever had `properties` injected non-empty (legacy blob lacking the `properties` key on first load+save; SEED clients with assets trigger it), a **later customAssets edit leaves `properties` STALE** → `getProperties`→**canonical `totalA` returns the OLD total**, while RatioContent (`:189`)/FinancialStatementsTab (`:190`) reading **raw `customAssets` are CURRENT/correct**. The two surfaces ISS-49 flagged are RIGHT; **`getProperties`/`totalA` is the stale path** — and it drives Summary, FullReport, AssetsLiabilitiesTab, the share portal + linked overview, AI export, AND `monthlyRows.net_worth` (`finance.js:98` → `client_monthly_summary` → dashboard trend). | **Re-scoped ⛔attended** (was 🟡). Fresh clients are SAFE — `mk` seeds `properties:[]` → empty → `getProperties` falls back to `customAssets` forever (harness CASE 1 clean). Reachable only for blobs whose `properties` was injected non-empty before a customAssets edit (harness CASE 2: getProperties/`totalA`=485,200 stale vs raw-customAssets=605,200 current). Fix lives in `mig`/`getProperties` (shared normalization + canonical money + the **cms save path**) and `properties` is in the portal sanitize allow-list (§2) → NOT an autonomous push. **Recommended fix:** in `mig`, re-derive `properties` from `cleanProps` every load (drop the `Array.isArray(c.properties)?c.properties:` preserve-branch) so the alias can never go stale — no real independent `properties` value exists to clobber. Queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES, attended). |
| ISS-48 | save-path / money | ⛔ | `saveHistoricalUpdate` (`clientReports.jsx:18`) recomputes a snapshot's `cashFlow` as `sumN − sumB − Σ(raw c.min)` — using **raw `card.min`** instead of `sumMin`/`effectiveMin` (canonical §3 cash flow = `sumN − sumB − sumMin`). `NMModal`'s new-month save (`:116`) correctly uses `sumMin`, so editing an existing month vs creating one can write **different** cashFlow for identical data. | Writes to `monthSnapshots` → **live save path** → ⛔attended per push-safety; do NOT autonomous-fix. Queued for an attended session. Found alongside ISS-46/47. |
| ISS-42 | calc / chart | 🟡 | `RetirementCalc` growth chart (`step=round(years/10)`) + ForecastCone (`round(years/8)`) sample at round steps → for the default 30→65 (35-yr) horizon the chart ends at **Yr 32** and the cone at **Age 62**, 3 yrs short of retirement, so the chart understates the headline scenario cards (which use full `totalMonths`) | Found in the 2026-06-26 deep item-1 scan of the 5 not-yet-reviewed calcs — all **headline formulas verified correct** vs `golden-anchor-logic §4`; this is the lone presentation gap. NOT documented as intended (unlike ISS-40). Fix = append the exact final point when the sampling loop misses it; additive, no headline math, headlessly verifiable. Visual output ⇒ owner-gated (§8) → queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES). |
| ISS-40 | calc / chart | 🟡 | `InterestCalc` summary "Final value" honors the compound-frequency selector (pf=12/4/1, v0.72.3); the `CompoundGrowthStack` chart below it always compounds monthly → chart endpoint disagrees with the headline number for Quarterly/Annual | **Documented** as intended in `golden-anchor-logic §4` ("the growth-stack chart still draws the monthly approximation") — so NOT a blind-fix. Fix is additive (optional `freq` prop on the chart, default 12 preserves all other behavior; only InterestCalc passes it) + headlessly math-verifiable. Queued as owner yes/no in CRUISE_QUESTIONS 2026-06-26. |
| ISS-38 | security / db | 🟡 | `set_updated_at` trigger fn has mutable `search_path` (Supabase advisor `0011`) | Low risk; fix = 1-line DDL `ALTER FUNCTION … SET search_path=''`. Prod-DB migration → attended/owner-greenlit, not autonomous. Queued in CRUISE_QUESTIONS 2026-06-26. |
| ISS-37 | security / auth | 🟡 | Leaked-password protection (HaveIBeenPwned) disabled in Supabase Auth | Owner dashboard toggle, free, no code. Rec: enable. Queued in CRUISE_QUESTIONS 2026-06-26. |
| ISS-09 | lint / hooks | 🟡 | `react-hooks/*` (static-components, rules-of-hooks, exhaustive-deps, set-state-in-effect) | Component-structure + hook-order (pitfalls #13/#17). Attended review only — NOT a safe autonomous bulk sweep. (One single, verifiable instance — the `CalculatorsPage` cascading-render — was fixed in isolation v0.83.13; the bulk remains attended.) |
| ISS-08 | lint / cosmetic | 🔴 | `no-unused-vars` ×199 + `no-empty` ×26 | Cosmetic debt in dense extracted code. Bulk patches need scope-aware review (D-36) — don't blind-fix. |
| ISS-07 | perf / build | 🔴 | Bundle >800 KB after minify (index ~1.3 MB) | Code-splitting / dynamic import opportunity. Single-bundle by D-1 history; needs a deliberate split plan. |
| ISS-06 | calc | 🟡 | `IncomeCalc` uses hardcoded 2025 tax brackets | Annual maintenance item — refresh brackets each tax year. |
| ISS-05 | dashboard | 🟡 | 3 placeholder charts: `billsStacked` / `billsYoY` / `payoffProgression` | Need bills-by-category + per-debt apr/min aggregates AND a dashboard.jsx re-wire. Owner: worth it? |
| ISS-04 | reminders | 🟡 | `promoExpiring` + per-bill/card "Client Due" reminders are blob-only | Need new summary columns + a backfill (save-path derivation change). Owner decision. |
| ISS-03 | scale | 🟢 | `color1` is a deterministic hash (originals not stored in summary) | Intended follow-up; accent re-derives stably. Revisit only if owner wants original accents preserved. |

## Whole-app multi-agent review — 2026-06-26 (39 findings, top 25 verified; 3 fixed in v0.83.8)
> Disposition tags: **⛔attended** = touches the live save path / needs backfill → NEVER autonomous-fix (CRUISE_MODE push-safety); **🟢loop-ok** = additive, no save-path → cruise loop may fix-and-push; **🟡owner** = product/policy decision (see CRUISE_QUESTIONS).

| ID | Area | Sev | Disp | One-line |
|---|---|---|---|---|
| ISS-12 | save-path | high | ⛔attended | `gaSaveClient` non-atomic find-then-insert; two racing first-saves of a new client → unique-violation → silent failed write (newer edits dropped, `selected` still shows them). |
| ISS-13 | save-path | high | ⛔attended | `client_monthly_summary` is upsert-only, never pruned → relabeling/removing a month leaves stale rows feeding `ga_dashboard_trend` + debtRising. |
| ISS-14 | save-path | high | ⛔attended | `gaDeleteClient`/`gaSetArchived` don't touch cms → deleted/archived clients still inflate the portfolio trend chart (RPC has no deleted_at/archived filter). |
| ISS-15 | save-path | med | ⛔attended | cms upsert failure is caught-and-logged after the blob write → time-series silently diverges; green "saved" toast still fires. |
| ISS-16 | save-path | med | ⛔attended | `importMultiple` ignores `gaSaveClient` result; an imported blob whose id collides with an existing `local_id` silently OVERWRITES that client (mig preserves incoming id). |
| ISS-17 | save-path | med | ⛔attended | Client bootstrap mints a fresh self-blob when `gaLoadClients` returns null (load ERROR, not empty) → duplicate self row, real data stranded. |
| ISS-18 | money / scale | med | ⛔attended | `monthlyRows` net_worth = savings(liquid)−debt ≠ canonical totalA−totalL (§3) → monthly net-worth trend understated/negative for asset-rich clients. Needs formula fix + backfill. |
| ISS-19 | security | high | 🟡owner | `api/_sanitize` allow-list filters TOP-LEVEL keys only; nested free-text (e.g. `customAssets[].desc`) passes verbatim to the public portal + linked clients. Owner: which nested fields are private? |
| ISS-20 | security | med | ⛔attended | `api/admin-members` grant/revoke authorized for ANY advisor (only `list` is admin-gated) → any self-registered advisor can comp Premium. Clean fix (gate behind `isAdmin`) but it's a permission change → attended, not autonomous. |
| ISS-21 | security | high | 🟡owner | Admin `list` gated by MUTABLE auth email, not a stable uid → email-change privilege-escalation if Supabase email-confirm is off. Fix: gate by uid (needs the real admin uids). |
| ISS-22 | security | med | 🟡owner | `resolve-portal` rate-limit is fail-open when Upstash unset (by design). Defense-in-depth only — token entropy makes brute force impractical. |
| ISS-23 | security | med | ⛔attended | `link.js` island snapshot uses `.limit(1)` with no order → arbitrary row frozen if a client account ever has >1 `clients` row (rare; client-role normally has one). |
| ISS-24 | product | med | 🟡owner | Signup lets anyone self-select "Advisor"; advisors are never Premium-gated → Free/Premium model bypassable at registration (D-13b territory). |
| ISS-25 | product | med | 🟡owner | PremiumUpgrade "I already subscribed — activate" flips `accountPlan` client-side with no server verify (known honor-system; tighten now that the webhook exists?). |
| ISS-26 | api / billing | med | 🟡owner | `stripe-webhook` grants premium from `client_reference_id` without checking `payment_status==='paid'`/`mode`, and no event de-dup → premium before capture / on replay. |
| ISS-27 | api / scale | 🟢 | `patchByEmail` listed only first 200 auth users → grant/revoke silently no-op'd past page 1. **FIXED** v0.83.11 (paginated like `loadClients`). |
| ISS-28 | calc | 🟢 | `HomeEquityCalc` "Months Saved" rounded payoff up to whole years → understated/0/negative. **FIXED** v0.83.9. |
| ISS-29 | calc | 🟢 | `HomeEquityCalc` "Interest Saved" was a fabricated approx unrelated to the amort table. **FIXED** v0.83.9. |
| ISS-30 | i18n (D-3) | 🟢 | `AmortTablePaginated` hardcoded English headers + "Yr". **FIXED** v0.83.10 (passed `t`, new keys). |
| ISS-31 | i18n (D-3) | 🟢 | `EquityTablePaginated` hardcoded English headers + "Yr". **FIXED** v0.83.10. |
| ISS-32 | i18n (D-3) | 🟢 | `clientCalcs` 5 "Prefilled from…" helper lines hardcoded. **FIXED** v0.83.10 ({n}-token replace). |
| ISS-33 | i18n (D-3) | 🟢 | `clientCalcs` "HOUSEHOLD COMBINED" block hardcoded. **FIXED** v0.83.10 (reused income keys + new header key). |

## Recently fixed (recurrence-watch — prune after a few clean cycles)

| ID | Area | Status | One-line | Shipped |
|---|---|---|---|---|
| ISS-61 | i18n (D-3) | 🟢 | **`components/clientData.jsx` advisor data-management modals wired titles via `t?.x` but rendered all body content hardcoded English.** This batch localized the two client-management **confirmation** modals: **DuplicateResolverModal** (duplicate-count summary, "POSSIBLE DUPLICATES" header, Incoming/Existing captions, `partner:` label, Merge/Skip/Import-as-New buttons, "N new clients will be imported.", Cancel/Apply) and **DeleteClientModal** ("This action is permanent", the {name}+{n}-months delete warning, Cancel, Delete Forever). Same class as ISS-30–33/55/57/58. +14 EN/ES keys (reused `cancel`); global-token `.replace()` pluralization (`{ps}`/`{ms}`/`{pe}`/`{mv}` driven by `count!==1`) keeps one key/language correct singular+plural; `<b>{name}</b>` emphasis preserved via pre/post fragments (`deleteClientWarnA`/`B`). **Pure display — stored payloads + delete/merge/save logic untouched** → autonomous-safe push (matches ISS-57/58). **Remaining sub-scope (future ticks):** `BackupImportModal`, `ExportModal` (SSN CSV → needs `golden-anchor-logic` guard), `ImportWizard` steps. Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1922/1922; node interpolation harness clean (no leftover tokens, EN/ES plurals correct). Found in the item-4 advisor-surface i18n scan. | v0.83.28 (2026-06-26) |
| ISS-59 | report / money / display | 🟢 | **`components/clientReports.jsx` itemized the per-card minimum from raw `c.min` in two report surfaces while their own footer used canonical `sumMin`** (= Σ `effectiveMin`), so the breakdown rows didn't reconcile to the total directly below. (1) **CashFlowStatement** (`:156`) "DEBT SERVICE" list filtered `c.min>0` + showed `value={c.min}` above "Total Debt Service"=`minD`; (2) **FullReport** (`:307`) per-card "Min Pay" column = `fmtD(c.min)` vs footer `fmtD(minD)`. Same class as ISS-44/46/48/50/56 — here in the report breakdown surfaces. Four modes: balance w/ unset min → row hidden but in total; paid-off card w/ stale min → phantom $0-balance row; below-$25-floor shown raw; min>balance uncapped. Fix: both use `effectiveMin(c)` for filter + value. | **Pure display, not save path** (NMModal/monthlyRows use `sumMin`; raw-min snapshot save is the separate ⛔ ISS-48) → autonomous-safe push (matches ISS-56). node harness `scratchpad/iss_cfmin.mjs` (4 cases): OLD rows $205 ≠ $230 footer → NEW $230 = footer. Gates: build clean; lint 427/408 = baseline (0 new); no new strings. Found in the item-1 `clientReports.jsx` raw-`.min` correctness scan. | v0.83.27 (2026-06-26) |
| ISS-58 | i18n (D-3) | 🟢 | **`components/clientModals.jsx` had ~30 hardcoded-English strings beyond the ISS-57 dropdowns** — all six data-entry modals (Card/Account/Loan/Bill/Asset/Split/Join + Income footer) rendered English regardless of language. Modal **edit-titles** (`${t.editLabel} Card/Account/Loan/Bill/Asset` → now `${t.editLabel} ${t.card/…}`, mirroring `IncomeModal`); CardModal's 4 promo `setErr` + save over-balance errors, "Suggested:", "Limit ($)", "(1-31, optional)", "e.g. 15"/"e.g. Balance Transfer" placeholders, "No promotional rates…", "at … % APR · ends …", "Add"; IncomeModal "Annual:"; AccountModal "e.g. Chase Checking" + liquid/investment/household captions; LoanModal "e.g. Honda Accord"; AssetModal "Equity:"/"Gain:"; SplitModal auto-assign note + "Day"/"items"; JoinModal "becomes Person 1…" + "Select a client first." alert. Same class as ISS-30–33 / ISS-55 / ISS-57. | **Pure display — stored `onSave` payloads, the `np.label\|\|"Promo"` stored default, and `/mo` untouched → not save path → autonomous-safe push (matches ISS-57).** +27 new EN/ES keys (reused `editLabel`/`annual`/`dayLbl`/`limitField`; the last two pre-existed → initial insert duplicated them, `no-dupe-keys` caught it, dupes removed). Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1908/1908. Found in the item-1 `clientModals.jsx` i18n scan. | v0.83.26 (2026-06-26) |
| ISS-57 | i18n (D-3) | 🟢 | **`CardModal` / `AccountModal` / `LoanModal` (`components/clientModals.jsx`) hardcoded the person/owner `<select>` labels** — `[["joint","Joint"],["p1",client?.firstName\|\|"Person 1"],["p2",client?.partnerFirst\|\|"Person 2"]]` — so the "Owed by"/"Owner" dropdown showed **English regardless of language**, while the sibling `IncomeModal`/`BillModal` already wire `t.joint`/`t.p1`/`t.p2` (same data-entry surface, inconsistent). Also `CardModal.save` raised a bare `setErr("Name required.")` vs siblings' `t.nameReqErr`. Same class as ISS-30–33 / ISS-55 (hardcoded English in extracted components). Fix: all three dropdowns → `t?.joint\|\|"Joint"` / `client?.firstName\|\|t?.p1\|\|"Person 1"` / `client?.partnerFirst\|\|t?.p2\|\|"Person 2"`; `CardModal` error reuses `t.nameReqErr`. **Pure display — stored `owedBy`/`owner` values unchanged → not save path → autonomous-safe push. Zero new keys** (reused `joint`/`p1`/`p2`/`nameReqErr`, present in both T.en/T.es) → EN/ES symmetry unchanged by construction. Gates: build clean; lint 427/408 = baseline (0 new). Found in the item-1 `clientModals.jsx` i18n scan. | v0.83.25 (2026-06-26) |
| ISS-56 | reminders / money / display | 🟢 | **`getClientRem` (`utils/finance.js:87`) — the client "Card Min" reminder — gated and displayed from raw `cc.min`** (`if(cc.min>0)` … `amount:cc.min`) instead of canonical `effectiveMin(cc)` (§3). The advisor path (`getAdvRem` highDSR) already uses `sumMin`/`effectiveMin`, so the two reminder surfaces diverged on identical data. Four failure modes: (1) card with balance but unset `min` → real reminder **hidden**; (2) paid-off card (`balance:0`) with stale `min` → **phantom** reminder for $0 balance; (3) below-$25-floor min shown raw; (4) min > balance shown raw (not capped). Same class as ISS-45/46/48/50, here in the client reminder surface. Fix: `const em=effectiveMin(cc); if(em>0) … amount:em`. **Pure display, not save path** (dismissal key `cardDue:{cid}:{cardId}:{YYYY-MM}` is amount-independent) → autonomous-safe push (disposition matches ISS-44). node harness `scratchpad/iss56.mjs` (5 cases) proves all four. Gates: build clean; lint 427/408 = baseline (0 new); no new strings. Found in the item-1 `utils/finance.js` reminder-engine correctness scan. | v0.83.24 (2026-06-26) |
| ISS-55 | i18n (D-3) | 🟢 | **`MarketInvestmentsSection` + `MarketInvestmentModal` (`clientSections.jsx`) had hardcoded English** — modal title (Edit/Add Market Investment), "＋ Add Investment", empty state "No market investments yet…", footer "Value:"/"Gain/Loss:", and the inline "{n} shares" unit all rendered EN-only regardless of language (D-3 violation). Same class as ISS-30–33 (calculator hardcoded English). The save alert "Ticker and name required." was hardcoded **despite `t.tickerNameReq` already existing**. Fix: 6 display edits wiring `T`; +7 EN/ES keys (`editMarketInv`/`addMarketInv`/`addInvestment`/`noMarketInv`/`valueColon`/`gainLoss`/`sharesUnit`), alert reuses `tickerNameReq`. Pure display, not save path → autonomous-safe push. Gates: build clean; lint no new (408 err); EN/ES 1881/1881. Found in the item-1 `clientSections.jsx` i18n scan. | v0.83.23 (2026-06-26) |
| ISS-54 | portal / money / display | 🟢 | **`pages/portal.jsx` share-portal overview AND `LinkedOverview` double-count property** in the asset-allocation donut: both compute the "Property & assets" slice `propV` as `[].concat(c.customAssets,c.properties)` (the **union**) but `customAssets`/`properties` are mutually-exclusive aliases — `getProperties(c)` (`finance.js:59`) chooses ONE and canonical `totalA` counts them **once, never both** (`golden-anchor-logic §6`). Donut center = `totalA(c)`, so unioning both makes the slices overstate property + not reconcile to the center whenever `c.properties` is non-empty (ISS-49-class legacy blobs → ~2× the property value; would become universal for any custom-asset client once the ISS-49 `mig` fix lands). Same root as ISS-49 (the alias) but a distinct, independent **display** double-count — **not** the save path → autonomous-safe push. Fix: both `propV` use canonical `getProperties(c)`. node harness `scratchpad/iss54.mjs`: legacy $200k home slices $480k→$280k (=center); fresh client unchanged. Found in the item-1 portal/LinkedOverview correctness scan (2026-06-26). | v0.83.22 (2026-06-26) |
| ISS-52 | export / money | 🟢 | The **AI client export** (`utils/aiExport.js`, `gaClientAIText`) never listed `marketInvestments`: its "Investments"/"Physical assets"/"Portfolio model" sections keyed on `c.investments`/`c.physicalAssets`/`c.portfolio` — **non-existent fields** (canonical = `marketInvestments`/`customAssets`/`savedPortfolio`, `golden-anchor-logic §6`), so they never fired. MI value IS in canonical `totalA` (the export's "Total assets"), so the AI saw an unreconcilable gap (e.g. $68k total vs $8k listed; $60k of holdings invisible). Same class as ISS-36/47/50. Fix: Investments section now reads `marketInvestments` (ticker/name/value/cat/shares/cost-basis/gain) + `savedPortfolio`; dead `physicalAssets` branch dropped (customAssets shown with `cat`). **Pure read-only export — not the save path** → autonomous-safe push. node harness `_aiexport_harness.mjs`: current omits $60k MI → fixed itemizes both, listed assets reconcile to `totalA` ($68k). Found in the item-1 `utils/aiExport.js` correctness scan. | v0.83.21 (2026-06-26) |
| ISS-50 | report / money / ratios | 🟢 | **Recurrence of ISS-46 + ISS-47 in the Summary report tab** (`SummarySection`, `clientReports.jsx:52` — the last report surface still on the non-canonical basis). (1) `mnd` (`:60`) summed **raw `Σ c.min`** instead of canonical `Σ effectiveMin(c)` → the Summary's **DSR**, **cash flow**, **Min Pay KPI**, income pie, and DSR recommendation diverged for no-min / paid-off-stale-min / below-$25-floor cards (contradicted the Financial-Statements DSR fixed in ISS-46). (2) `totA` (`:72`, Health-Score radar D/A axis) omitted **`marketInvestments`** (ISS-47 class) → DTA overstated for MI-holding clients. Both pure display derivations, **not** the save path (NMModal already uses `sumMin`; the raw-min save path is the separate ⛔ ISS-48). Fix: `effectiveMin` + add MI sum. node harness `iss50.mjs`: `mnd 160→125`, `totA 491k→611k`. | v0.83.20 (2026-06-26) |
| ISS-47 | report / money | 🟢 | `RatioContent` (`clientReports.jsx:189`) and the **Balance Sheet** of `FinancialStatementsTab` (`:190-191`) computed `totalA` as accounts + customAssets only — `marketInvestments` omitted (canonical §3 `totalA` includes it; `AssetsLiabilitiesTab` `:566` already did). Total-Assets KPI, Net Worth, and DTA were **understated** for any client holding market investments, and the holdings never appeared as a balance-sheet line. Fix: both `tA` derivations now add `Σ marketInvestments.value` (view-scaled like `customAssets`); MI holdings render as `📈 {ticker} {name}` rows inside the existing **Investment Assets** section with the **Total Investment** subtotal reconciled to `invA+miA` (no new UI string — folded into the existing section). Pure display, not save path. node harness: both `360k→393k`=canonical, DTA `56.94%→52.16%`, both surfaces now agree. | v0.83.19 (2026-06-26) |
| ISS-46 | report / ratios / money | 🟢 | `RatioContent` (`clientReports.jsx:189`, also Full Report `:311`) computed the DSR numerator from raw `Σ card.min` instead of canonical `Σ effectiveMin(card)` (§3 `sumMin`). Diverged for no-min cards (understated), paid-off cards w/ stale min (overstated), and below-$25-floor mins; same `minD` also fed the RSR available-cash base. Contradicted the Cash Flow Statement's DSR (`:144`, uses `sumMin`) on the same tab — ISS-45 class. Fix: `minD=cards.reduce((s,c)=>s+effectiveMin(c),0)`. Pure display, not save path. node harness 4 cases. | v0.83.18 (2026-06-26) |
| ISS-45 | calc / ratios / display | 🟢 | `ratFmt("dsr")` rendered the debt-service ratio as a **`×` multiple** (`0.36x`) because `dsr` was grouped with `currentRatio` in the formatter, instead of a **percentage** (`36%`). Visible in `RatioContent` (Financial Statements → Ratios) where the DSR card showed `0.36x` next to its own `< 36%` benchmark — contradicting every other DSR surface (RadialGauge/reminder both `*100+"%"`) and the canonical model (`golden-anchor-logic §3`). Fix: split the branch — `currentRatio` keeps `×` (unchanged), `dsr` → `(v*100).toFixed(0)+"%"` with `v>=1→"99+%"` for the no-income sentinel. Also removed the dead `const abs` in that branch (−1 `no-unused-vars`). Pure display derivation, not the save path. node harness: dsr→%, currentRatio proven unchanged. | v0.83.17 (2026-06-26) |
| ISS-44 | reminders / dates | 🟢 | `getClientRem` days-until-due used a **hardcoded 31** for the bill month-wrap (over-counts in 28/30-day months → bills due within 7 *real* days got hidden by the `diff<=7` filter near month-end) and `Math.max(0,dueDay-day)` for cards (no wrap → past-due-this-month cards showed `0`, sorting to the top). New `_daysUntilDue(dueDay,today)` helper computes the exact delta with real month lengths + day clamp + Dec→Jan rollover; both paths call it. Read-only display derivation (not the save path); keys/dismissals embed `YYYY-MM`, unaffected. node harness 8/8 edge cases. | v0.83.16 (2026-06-26) |
| ISS-43 | i18n (D-3) / fmt | 🟢 | `fmtS` (abbreviated `$5K`/`$1.2M`, 13 call sites) hardcoded `"$"` while `fmt` honors the user's currency (`_GA_CCY`) → non-USD picks (EUR/GBP/MXN/CAD) showed `$` on abbreviated values, `€`/`£`/`MX$` elsewhere. Now derives the symbol from the same Intl config `fmt` uses (`_ccySym`), K/M rounding unchanged. Found in the item-1 `utils/finance.js` helper scan (EN/ES symmetry audit clean 1874/1874). | v0.83.15 (2026-06-26) |
| ISS-41 | a11y (WCAG 4.1.2) | 🟢 | 3 icon-only `×` buttons had no accessible name (shared `Modal` close → all 44 modals; `IAdd` cancel; Intake detail close). Added bilingual `aria-label`+`title` via a `gaLabel` helper (reads `<html lang>`, reuses `close`/`cancel` keys). **Footgun: shared primitives don't receive `t` — resolve labels from `document.documentElement.lang`, kept in sync since v0.83.12.** | v0.83.14 (2026-06-26) |
| ISS-27 | api / scale | 🟢 | `admin-members` grant/revoke `patchByEmail` paged only first 200 auth users → silent no-op past #200; now paginates like `loadClients` | v0.83.11 (2026-06-26) |
| ISS-30–33 | i18n (D-3) | 🟢 | Calculator hardcoded-English (amort/equity table headers + clientCalcs prefill helpers + household block) → bilingual; 14 new keys EN+ES, 4 reused | v0.83.10 (2026-06-26) |
| ISS-29 | review / calc | 🟢 | `HomeEquityCalc` Interest Saved fabricated approx → now real (baseInt − extraInt) from shared amort loop | v0.83.9 (2026-06-26) |
| ISS-28 | review / calc | 🟢 | `HomeEquityCalc` Months Saved year-rounded vs exact → understated/0/neg; now exact-month difference | v0.83.9 (2026-06-26) |
| ISS-36 | review / money | 🟢 | aiExport card "min" called `payM(cd)` (wrong fn+arity) → always $0; now `effectiveMin(cd)` | v0.83.8 (2026-06-26) |
| ISS-35 | review / calc | 🟢 | `SavingsCalc` 0% APY divided by rate → NaN→$0; now guarded to simple sum | v0.83.8 (2026-06-26) |
| ISS-39 | review / calc-nav | 🟢 | `CalculatorsPage` synced `active`←`activeCalc` in a `useEffect` (synchronous setState → cascading render); on browser back/forward the prior view flashed one frame. Now derived during render (React "adjust state on prop change") — no flash, no effect. −2 lint errors. **Verified** deep-link + tile + back/forward on dev preview. | v0.83.13 (2026-06-26) |
| ISS-34 | review / nav | 🟢 | advisor Back/Forward compared string summary-id `===` number selectedId; now `String()===String()` | v0.83.8 (2026-06-26) |
| ISS-11 | lint / theme | 🟢 | `no-misleading-character-class` ×3 in `stripLeadEmoji` (invisible FE0F/ZWJ/20E3 in source) | 2026-06-26 — rewrote literal combining chars as `\u{…}` escapes + justified disable; behavior identical. **Footgun pattern: never leave raw combining/ZWJ code points in source — they resist byte patching; use `\u{…}` escapes.** |
| ISS-10 | security / deps | 🟢 | `npm audit` 4 vulns (form-data/ws/js-yaml/@babel) → 0 | 2026-06-26 — `npm audit fix` (lock-only; puppeteer-core unchanged, ws patch 8.20.1→8.21.0). Owner optional: PDF spot-check on prod. |
| ISS-02 | extraction / imports | 🟢 | Missing imports after Phase-2 carve-out → `ReferenceError` (admin.jsx `expBackup`; import.js `MS`; chartEditors `dashChartOptions`) | v0.83.3 / v0.83.6 / v0.83.7 — **recurrence pattern: watch every extraction for lost imports.** |
| ISS-01 | save path | 🟢 | Advisor save-success toast fired unconditionally — a failed save showed "✓ saved" (silent data-loss risk) | v0.83.1 (shipped to main 2026-06-25, owner-approved in test-mode) |

> **Recurrence pattern to watch (from ISS-02):** Phase-2/3 file extractions repeatedly drop a
> module-level import that was implicit in the old single-file scope. Any future extraction MUST
> re-run `npm run lint` and confirm `no-undef` is **0** before commit.
