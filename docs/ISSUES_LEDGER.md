# ISSUES_LEDGER.md — Golden Anchor Finance

> Brief, one-line-per-issue tracker of known problems + their history. The review handler
> (`finance-review-mode`, §4c) checks this **backward**: a new report that matches an existing
> entry is a **recurrence** — root-cause the old fix, don't re-paste it. Keep entries terse;
> the "why" of any shipped fix lives in `CHANGELOG.md`. Newest on top.
>
> Status: 🔴 open · 🟡 owner-decision · 🟢 fixed (kept briefly for recurrence-matching, then pruned) · 🔁 recurrence

## Open

| ID | Area | Status | One-line | Notes / owner question |
|---|---|---|---|---|
| ISS-88 | a11y / forms (WCAG 1.3.1 / 4.1.2, Level A) | 🟡 | **App-wide: form inputs have no programmatic label association → no accessible name for screen readers.** The shared `Field` primitive (`components/primitives.jsx:190`) renders its `<label>` as a **sibling** of the input child with **no `htmlFor`** (and the input child carries no `id`), so the visible label is not programmatically tied to the control. `htmlFor` appears **nowhere** in `src` (grep = 0), and only **8 of 175** `<input>` elements carry their own `aria-label`. The remaining ~150+ inputs (every NewClientModal / ClientForm / CardModal / report editor / profile / intake field that routes through `Field`, plus the ad-hoc `<div><label …></label><input>` blocks in `clientEditor.jsx:26/27/41`) expose an **empty accessible name** — a screen reader announces "edit text" with no field name. Placeholders (where present) are not a label substitute (SC 1.3.5/4.1.2) and vanish on input. Same objective-a11y family as ISS-72/83/85/86 (which closed icon-button names + autocomplete) but this is the **text-input naming** gap those sweeps didn't reach. | **🟡owner-gated / ⚠️attended — NOT an autonomous push.** The fix lives in the **central shared `Field` primitive** (DEPENDENCY-MAP: high-impact shared code) → blast radius is ~150 inputs across **every form in the app**, and accessible-name resolution can't be headlessly verified across all sites in an unattended tick → per push-safety "anything you're unsure of → queue" + §8 (shared-surface change). **Recommended fix (additive):** in `Field`, `const id=useId();` set `<label htmlFor={id}>` and inject the id into the child via `React.cloneElement(children,{id:children.props.id\|\|id})` — clean for raw `<input>`/`<select>` and the `{...rest}`-spreading custom inputs (`NumInp`/`MaskedNumInp`/`YearInp`); `SSNInput` (no `...rest`) + `Row2` children are harmless no-ops (no worse than today). Then convert the 3 ad-hoc `clientEditor.jsx` blocks to `Field`/`htmlFor`. Headlessly re-verify with Playwright `getByLabel()` on a sample of forms post-change. Queued yes/no in CRUISE_QUESTIONS 2026-06-27 (Rec: YES, attended). Found in the 2026-06-27 item-4 objective-a11y input-labeling sweep (after confirming aria-label/title/alt/placeholder i18n all clean — no hardcoded-English a11y attributes). |
| ISS-87 | report / money / display | 🟡 | **`DebtSection` editing grid (`components/clientSections.jsx:19`) shows per-row Min Pay = `fmtD(c.min)` (raw stated min) and Payoff = `payM(c.balance,c.apr,c.min)` (raw `c.min` as the payment), while its own footer Total = `fmtD(sumMin(client.cards))` (canonical Σ`effectiveMin`).** So the Min-Pay column rows don't reconcile to the Total directly below them — the exact four-mode divergence ISS-59 fixed: (1) unset/0 min → raw `$0` row but `effectiveMin`=`round(1%·bal+cardMoInt)` in the total (e.g. $5k bal/20% apr → ~$133); (2) paid-off card w/ stale min → raw `$50` row but `$0` in the total (balance≤0); (3) below-$25 floor shown raw; (4) min>balance shown uncapped. The **Payoff** column compounds it — a 0-min card shows "—/never pays off" (`payM` payment ≤ interest → null) when `effectiveMin` would actually amortize it. Same root + class as the report-surface **ISS-59** (which was 🟢 autonomous-fixed in `clientReports.jsx`), but here in the **advisor editing grid**. | **🟡owner-decision (display, NOT save path)** — `save`/`onUpdate` write the raw card objects (the CardModal sets `c.min`); changing the grid's *displayed* min/payoff doesn't touch what's persisted (same disposition as ISS-59). Queued-not-pushed because it changes a visible number on the **primary advisor editing surface** AND, unlike the read-only report ISS-59, an editing grid has a legitimate "show the value the advisor actually entered" counter-argument → per push-safety "anything you're unsure of → queue." **Recommended fix:** per-row `fmtD(effectiveMin(c))` + `payM(c.balance,c.apr,effectiveMin(c))` so the column reconciles with its footer and matches the canonical minimum used everywhere in cash flow (the modal still edits the raw `min`); additive, headlessly verifiable. Queued yes/no in CRUISE_QUESTIONS 2026-06-27 (Rec: YES). Found in the 2026-06-27 item-1 fresh correctness scan of `clientSections.jsx` (a never-money-traced surface; all aggregate metrics — `sumN`/`sumG` income footers, `sumB` bills total, `sumMin` debt footer, `totalMoInt`, `liquidA`, `totalA`/`totalL`/net worth, EF target, allocation `avail`=`sumN−sumB−sumMin` — verified canonical vs §3; the per-row card min/payoff display was the lone divergence). |
| ISS-81 | save-path / money / scale / dashboard | ⛔ | **`monthlyRows` (`utils/finance.js:120-122`) partitions only `accounts` (→liquid/invest/other) + `customAssets` (→property) into the per-month `client_monthly_summary` asset buckets — `marketInvestments` lands in NO bucket**, so `a_liquid+a_invest+a_property+a_other = totalA − ΣMI`, **not** canonical `totalA` (§3, which counts MI). The advisor dashboard reconstructs per-month net worth from exactly those four buckets in **three slots** — `netWorthBridge` (`components/dashboard.jsx:342`, the invest band understated by MI throughout), the `netWorthForecast` **history** (`:403`), and `kpiSparklines` (`:485`) — while each slot's live `▶ Now`/`liveNW` point uses `S.total_nw` = `Σ(totalA−totalL)` with **MI included**. Net effect for any MI-holding client: the portfolio net-worth history line sits low every historical month then **jumps up at "Now"** by the aggregate MI amount — an **ISS-80-class phantom jump** — and the NetWorthBridge invest band omits market holdings across all months. Distinct from ISS-18 (which fixes the separate `net_worth` *column* `:130`; the forecast/sparkline ignore that column and re-sum the buckets, so ISS-18's fix alone does NOT close this). Same MI-omission *class* as ISS-53 (`clientSummary.assets` sunburst) but in the per-month cms rows. | **⛔attended** — writes `client_monthly_summary` (scale-data-layer save path) **and** needs a backfill of existing rows (already-persisted `a_invest` omits MI), same disposition as ISS-18/53/60. **Recommended fix:** in `monthlyRows`, add `aInvest += (d.marketInvestments\|\|[]).reduce((a,x)=>a+(+x.value\|\|0),0)` so MI joins the invest band and the four buckets sum to `totalA(snapshot)` — mirrors the ISS-53 fix (MI→"invest" bucket), purely additive. **Bundle with the ISS-18 + ISS-60 backfill** (same fn, same row, one pass). Reachable for any client whose monthly snapshots carry `data` with MI holdings (summary-only and Excel-imported snapshots have empty/no MI → unaffected). Queued yes/no in CRUISE_QUESTIONS 2026-06-27 (Rec: YES, attended). Found in the 2026-06-27 item-1 systematic `marketInvestments`-omission sweep — **refines the ISS-80 question's parenthetical** ("the net-worth reconstructions include `a_invest`, no MI omission"): the dashboard *consumer* reads `a_invest`, but the *producer* (`monthlyRows`) omits MI from it. |
| ISS-80 | dashboard / display / chart | 🟡 | **`debtVsSavingsTrend` dashboard slot (`components/dashboard.jsx:350-352`) plots card-only debt for history but total debt for the live point** — historical points use `debt:+r.l_cards\|\|0` (revolving cards only) while the appended `▶ Now` point uses `debt:Math.round(td)` where `td = S.total_debt` (cards **+** loans, `:226`). The chart is titled "Debt vs Savings Trend" (legend "Debt") and the dashboard's Total-Debt KPI is `S.total_debt`, so an advisor with any installment debt (mortgage/auto/student) sees the trend line sit low across all months then **jump up at "Now"** purely because loans appear only in the final point — reads as a debt spike that never happened. The per-month rollup row already carries `l_loans_all` (used 2 lines down in `netWorthBridge` `:342` and in the net-worth series `:403/:485`), so total-debt history is trivially available. An in-code `// CAVEAT:` explains the *live-point* total-debt choice but not the card-only history, leaving intent ambiguous. | **🟡owner-decision (display, NOT save path)** — `dashboard.jsx` is pure read of already-persisted rollups (no write), so a fix is the same autonomous-safe class as ISS-54/59; queued-not-pushed only because it **materially changes a visible main-dashboard chart** AND the caveat suggests the choice may be deliberate (revolving-only "behavioral" debt) → per push-safety "anything you're unsure of → queue," same handling as the documented-intent chart mismatches ISS-40/42. **Recommended fix:** `debt:(+r.l_cards\|\|0)+(+r.l_loans_all\|\|0)` so history matches the live point and the chart title (total debt throughout) — trivial, additive, headlessly verifiable. Queued yes/no in CRUISE_QUESTIONS 2026-06-27 (Rec: YES). Found in the 2026-06-27 item-1 fresh correctness scan of `dashboard.jsx` (a surface prior scans hadn't traced; its ratio math — DSR/savings-rate/EF/DTA — verified correct vs `golden-anchor-logic §3`). |
| ISS-60 | save-path / money / scale | ⛔ | **`monthlyRows` (`finance.js:118`) computes `cardMins = Σ (+cd.min\|\|0)` (raw min) and writes `spending = bills + cardMins` into the persisted `client_monthly_summary` row** — canonical debt service is `sumMin` = Σ `effectiveMin` (§3), so the stored `spending` series diverges for the same four cases as ISS-59 (unset min, paid-off stale min, sub-$25 floor, min>balance). Save-path sibling of the display-only ISS-59. | **⛔attended** — persisted cms summary derivation over snapshot data + needs a row backfill, same disposition as ISS-18 (`monthlyRows.net_worth`). NOT autonomous. **Recommended fix:** `cardMins = cards.reduce((a,cd)=>a+effectiveMin(cd),0)` — bundle with the ISS-18 backfill (same fn/row/pass). Queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES, attended). Found tracing the ISS-59 raw-`.min` scan into the save path. |
| ISS-53 | save-path / scale / dashboard | ⛔ | **`clientSummary.assets` (`finance.js:107-110`) omits `marketInvestments`** from the dashboard Asset-Sunburst breakdown — it spreads `accounts` (bucketed by `ACCT_META`) + `customAssets` (bucket "property") but **not** MI holdings. Yet the SAME function's `net_worth` (`:98`) uses canonical `totalA(c)` which **includes** MI → on the advisor dashboard the net-worth KPI counts market investments but the asset-allocation sunburst doesn't show them (an MI-heavy client's allocation chart understates invested assets and won't reconcile to net worth). Same omission class as ISS-47/50/52, but here it feeds the **`client_summary` row** (scale data layer save path) → understated/missing only in the persisted summary, fixed on a re-save. | **⛔attended** — writes `client_summary` via the save path + needs a backfill of existing rows (consistent with ISS-18 `monthlyRows` net_worth ⛔). **Recommended fix:** add an `invest`-bucket spread of `marketInvestments` (`{bucket:"invest",name:ticker||name,val:value}`) to the `assets` array — purely additive, mirrors the existing `customAssets` spread. NOT an autonomous push (save-path + backfill). Found in the item-1 `utils/aiExport.js`/`finance.js` correctness scan (2026-06-26). |
| ISS-51 | save-path / money / import | ⛔ | **`smartMerge` (`utils/import.js:222`) drops `marketInvestments` on duplicate-client merge.** Its array-union list is `["incomeStreams","bills","cards","accounts","loans","customAssets"]` — `marketInvestments` is **absent**, so `merged={...existing}` keeps the existing blob's MI and the **incoming** client's MI holdings are never unioned in. `marketInvestments` IS canonical (`mk`/`mig` seed it, `totalA` includes it — same omission class as ISS-47/50, but here it's **persisted**, not display). Both call sites feed the live save path: backup-restore merge mode `gaSaveClient(…,smartMerge(mig(full),bc))` (`App.jsx:715`) and import merge `smartMerge(dup,bc)` (`App.jsx:718`). Backup blobs round-trip MI (`expBackup` writes raw `clients`; `validateBackup` only checks the envelope), so incoming genuinely carries it. Restore/import a dup in merge mode → MI silently lost → understated assets/net worth. | **⛔attended** — writes via `gaSaveClient` (live save path), can't verify the end-to-end restore headlessly (no backup fixtures; prod-mutation classifier blocks it) → NOT an autonomous push; consistent with ISS-16 (`importMultiple`) ⛔. node harness `scratchpad/iss51.mjs` (replicates smartMerge verbatim): current drops $60k/2 holdings→0; one-key fix preserves both. **Recommended fix:** add `"marketInvestments"` to `arrKeys` (same union-by-id as the other five; purely additive). Queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES, attended). Found in the item-1 `utils/import.js` correctness scan. |
| ISS-49 | report / money / save-path | ⛔ | **Premise INVERTED — harness-proven 2026-06-26 (write-path traced as the ISS-49 note asked).** `properties[]` has **no user-edit write path** anywhere (grep across `src`: the only writes are `mk()`→`[]` and `mig()`). So `getProperties(c)` (`finance.js:59` = `c.properties?.length ? properties : customAssets`) prefers a **phantom alias** that `mig` freezes to a one-time snapshot of `customAssets` and thereafter preserves verbatim (`properties:Array.isArray(c.properties)?c.properties:cleanProps`; `upClient` runs `mig` before every save, so the stale value persists). `CustomAssetsSection.save` (`clientSections.jsx:27`) writes `customAssets` only. Result: for any blob that ever had `properties` injected non-empty (legacy blob lacking the `properties` key on first load+save; SEED clients with assets trigger it), a **later customAssets edit leaves `properties` STALE** → `getProperties`→**canonical `totalA` returns the OLD total**, while RatioContent (`:189`)/FinancialStatementsTab (`:190`) reading **raw `customAssets` are CURRENT/correct**. The two surfaces ISS-49 flagged are RIGHT; **`getProperties`/`totalA` is the stale path** — and it drives Summary, FullReport, AssetsLiabilitiesTab, the share portal + linked overview, AI export, AND `monthlyRows.net_worth` (`finance.js:98` → `client_monthly_summary` → dashboard trend). | **Re-scoped ⛔attended** (was 🟡). Fresh clients are SAFE — `mk` seeds `properties:[]` → empty → `getProperties` falls back to `customAssets` forever (harness CASE 1 clean). Reachable only for blobs whose `properties` was injected non-empty before a customAssets edit (harness CASE 2: getProperties/`totalA`=485,200 stale vs raw-customAssets=605,200 current). Fix lives in `mig`/`getProperties` (shared normalization + canonical money + the **cms save path**) and `properties` is in the portal sanitize allow-list (§2) → NOT an autonomous push. **Recommended fix:** in `mig`, re-derive `properties` from `cleanProps` every load (drop the `Array.isArray(c.properties)?c.properties:` preserve-branch) so the alias can never go stale — no real independent `properties` value exists to clobber. Queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES, attended). |
| ISS-48 | save-path / money | ⛔ | `saveHistoricalUpdate` (`clientReports.jsx:18`) recomputes a snapshot's `cashFlow` as `sumN − sumB − Σ(raw c.min)` — using **raw `card.min`** instead of `sumMin`/`effectiveMin` (canonical §3 cash flow = `sumN − sumB − sumMin`). `NMModal`'s new-month save (`:116`) correctly uses `sumMin`, so editing an existing month vs creating one can write **different** cashFlow for identical data. **Sibling site (`utils/import.js:160`):** the Excel-import snapshot builder computes `mt = sc.reduce((s,c)=>s+(+c.min\|\|0),0)` (raw min) and writes `cashFlow:Math.round(net-bt-mt)` into each imported `monthSnapshots` row (`:163`) → an imported month's cashFlow uses raw min, but the same month later edited via `NMModal` re-derives with `sumMin` → divergence on identical data, same root as the `saveHistoricalUpdate` case. **Second divergence in the SAME function-pair — the snapshot `debt` field (found 2026-06-27 item-1 scan of `clientReports.jsx`):** `saveHistoricalUpdate` (`:18`) writes `debt = Σ card.balance + Σ loan.balance` (cards **+ loans**), but BOTH other snapshot-write sites store **card-only** debt — `NMModal` (`:116`) `debt = Σ card.balance` and Excel-import (`import.js:159`) `dt = Σ card.balance` — and `SummarySection`'s live `▶ Now` trend point (`:62`) is also card-only. So editing an existing month rewrites its `debt` to *include* loan balances, diverging from how that month was created/imported and from the live trend point → for any client with installment loans, editing a historical month inflates **that month's** debt point only, a phantom spike in the trend. Same "divergence on identical data" root as the cashFlow case above, same two functions. **The target convention is owner-gated via ISS-80** (card-only "revolving" vs total cards+loans debt in the trend) — whatever is decided there must be applied consistently to all THREE write sites. | Writes to `monthSnapshots` → **live save path** → ⛔attended per push-safety; do NOT autonomous-fix. Queued for an attended session — **fix all sites in one pass**: cashFlow → `effectiveMin` at `clientReports.jsx:18` + `import.js:160`; `debt` field → align `saveHistoricalUpdate:18` / `NMModal:116` / `import.js:159` to the ISS-80 decision (card-only is the current 2-of-3 + live-point convention → make `saveHistoricalUpdate` drop the loan term to match; flip all three if owner chooses total debt). Backfill not strictly needed (both fields re-derive on any later month edit), but a re-save normalizes existing imported snapshots. Found alongside ISS-46/47; import sibling traced in the 2026-06-27 item-1 raw-`.min` re-scan; `debt`-field divergence traced in the 2026-06-27 item-1 `clientReports.jsx` save-path re-scan (depends on ISS-80). |
| ISS-42 | calc / chart | 🟡 | `RetirementCalc` growth chart (`step=round(years/10)`) + ForecastCone (`round(years/8)`) sample at round steps → for the default 30→65 (35-yr) horizon the chart ends at **Yr 32** and the cone at **Age 62**, 3 yrs short of retirement, so the chart understates the headline scenario cards (which use full `totalMonths`). **Third sibling site (`calculators.jsx:391`, `InflationCalc`):** the buying-power area chart loops `for(let y=0;y<=years;y+=Math.max(1,Math.round(years/10)))` → for horizons not divisible by the step it ends short of the headline year (node-verified: 25yr→last point Yr24, 35yr→Yr32), while the headline "What it will buy then" uses the full `years` — same round-step endpoint gap. | Found in the 2026-06-26 deep item-1 scan of the 5 not-yet-reviewed calcs — all **headline formulas verified correct** vs `golden-anchor-logic §4`; this is the lone presentation gap. NOT documented as intended (unlike ISS-40). Fix = append the exact final point when the sampling loop misses it (**apply to all three sites in one pass**: RetirementCalc, ForecastCone, InflationCalc); additive, no headline math, headlessly verifiable. Visual output ⇒ owner-gated (§8) → queued yes/no in CRUISE_QUESTIONS 2026-06-26 (Rec: YES). InflationCalc sibling traced in the 2026-06-27 item-1 scan of the two undocumented calcs (DIME + Inflation, both verified correct; now documented in `golden-anchor-logic §4`). |
| ISS-40 | calc / chart | 🟡 | `InterestCalc` summary "Final value" honors the compound-frequency selector (pf=12/4/1, v0.72.3); the `CompoundGrowthStack` chart below it always compounds monthly → chart endpoint disagrees with the headline number for Quarterly/Annual | **Documented** as intended in `golden-anchor-logic §4` ("the growth-stack chart still draws the monthly approximation") — so NOT a blind-fix. Fix is additive (optional `freq` prop on the chart, default 12 preserves all other behavior; only InterestCalc passes it) + headlessly math-verifiable. Queued as owner yes/no in CRUISE_QUESTIONS 2026-06-26. |
| ISS-38 | security / db | 🟡 | `set_updated_at` trigger fn has mutable `search_path` (Supabase advisor `0011`) | Low risk; fix = 1-line DDL `ALTER FUNCTION … SET search_path=''`. Prod-DB migration → attended/owner-greenlit, not autonomous. Queued in CRUISE_QUESTIONS 2026-06-26. |
| ISS-37 | security / auth | 🟡 | Leaked-password protection (HaveIBeenPwned) disabled in Supabase Auth | Owner dashboard toggle, free, no code. Rec: enable. Queued in CRUISE_QUESTIONS 2026-06-26. |
| ISS-09 | lint / hooks | 🟡 | `react-hooks/*` (static-components, rules-of-hooks, exhaustive-deps, set-state-in-effect) | Component-structure + hook-order (pitfalls #13/#17). Attended review only — NOT a safe autonomous bulk sweep. (One single, verifiable instance — the `CalculatorsPage` cascading-render — was fixed in isolation v0.83.13; the bulk remains attended.) |
| ISS-08 | lint / cosmetic | 🔴 | `no-unused-vars` ×99 + `no-empty` ×23 (**3 intentional empty `catch{}` documented in `api/link.js` 2026-06-27** — the account-linking path: line 94 (own-island snapshot read), 105 (auto-revoke stale portal tokens), 129 (advisor display-info read) are all deliberate graceful-degradation where a `null`/no-op fallback is the correct behavior; full path re-audited against `golden-anchor-logic` §2b — role gates + email hard-match + sanitize-only reads all confirmed intact. Each got an intent comment: clears the lint *and* makes the deliberate error-swallowing explicit for the next auditor; standalone `catch` lines, no logic change, no marker bump. Gate: build clean, total 329→326, only `no-empty` moved (26→23), `no-undef` stayed 0, all other rule counts unchanged. The remaining 23 `no-empty` are mostly packed into dense high-column one-liners — defer to attended. `no-unused-vars` was ×106; **7 dead leftovers removed across extracted files 2026-06-27**: 3 dead React-hook imports — `useEffect`+`useRef` in `components/charts.jsx`, `useCallback` in `components/primitives.jsx` (each appeared only in its import; build keeps `useState`/`useMemo`/`useEffect`/`useRef` as used) — plus 2 fully-dead `useState` pairs whose value AND setter were both eslint-flagged + grep-confirmed single-occurrence: `newRowModal`/`setNewRowModal` (`components/clientReports.jsx:458`) and `clearConfirm`/`setClearConfirm` (`pages/intake.jsx:653`); each a standalone line (no packed-line surgery). Gate: `no-undef` re-lint stayed 0, total 336→329, only no-unused-vars moved (106→99), all other rule counts unchanged. **Deliberately NOT removed:** the dead `doClearByStatus` bulk-delete helper (`intake.jsx:675`) + its sole-consumer import `gaDeleteIntakeSubmissionsByStatus` — staged *destructive* capability; deleting app capability is an attended judgment call, not an autonomous dead-code sweep. The dense-packed `App.jsx:242` (`clientsSort`) + `clientData.jsx:217` (`confirmReplace`) dead pairs left for a future tick (>250-col lines → Edit risk outweighs 2-error payoff). Prior: was ×195; **89 dead named imports removed from `src/App.jsx` 2026-06-27** — D-37-extraction leftovers across 13 import lines: 4 React hooks `useMemo`/`useId`/`createContext`/`useContext`, 20 intake/portal/report service fns, 6 theme `m*` table-style helpers, the whole `hooks/anim` import, 38 `utils/finance` helpers, plus `utils/import`/`engagementLetterTemplate`/`primitives`/`calculators`/`premium`/`intake`/`constants/meta` deadweight; each eslint-confirmed zero-reference + grep-verified single-occurrence; **post-edit project `no-undef`=0** so no used binding dropped, full rule histogram unchanged except no-unused-vars −89. Earlier: ×199→×197 via 2 dead `cardDebt`/`loanDebt` in `utils/aiExport.js`) | Cosmetic debt in dense extracted code. Bulk patches need scope-aware review (D-36) — don't blind-fix; **dead-import removal is the safest sub-class** (eslint scope analysis authoritative + `no-undef` re-lint is the gate, since esbuild won't flag a wrongly-removed import). Chip away with verified precise removals only. |
| ISS-07 | perf / build | 🔴 | Bundle >800 KB after minify (index ~1.3 MB) | Code-splitting / dynamic import opportunity. Single-bundle by D-1 history; needs a deliberate split plan. |
| ISS-06 | calc | 🟡 | `IncomeCalc` uses hardcoded 2025 tax brackets | Annual maintenance item — refresh brackets each tax year. |
| ISS-05 | dashboard | 🟡 | 3 placeholder charts: `billsStacked` / `billsYoY` / `payoffProgression` | Need bills-by-category + per-debt apr/min aggregates AND a dashboard.jsx re-wire. Owner: worth it? |
| ISS-04 | reminders | 🟡 | `promoExpiring` + per-bill/card "Client Due" reminders are blob-only | Need new summary columns + a backfill (save-path derivation change). Owner decision. |
| ISS-03 | scale | 🟢 | `color1` is a deterministic hash (originals not stored in summary) | Intended follow-up; accent re-derives stably. Revisit only if owner wants original accents preserved. |

## Whole-app multi-agent review — 2026-06-26 (39 findings, top 25 verified; 3 fixed in v0.83.8)
> Disposition tags: **⛔attended** = touches the live save path / needs backfill → NEVER autonomous-fix (CRUISE_MODE push-safety); **🟢loop-ok** = additive, no save-path → cruise loop may fix-and-push; **🟡owner** = product/policy decision (see CRUISE_QUESTIONS).

| ID | Area | Sev | Disp | One-line |
|---|---|---|---|---|
| ISS-12 | save-path | high | ⛔attended | `gaSaveClient` non-atomic find-then-insert; two racing first-saves of a new client → unique-violation → silent failed write (newer edits dropped, `selected` still shows them). |
| ISS-13 | save-path | high | ⛔attended | `client_monthly_summary` is upsert-only, never pruned → relabeling/removing a month leaves stale rows feeding `ga_dashboard_trend` + debtRising. |
| ISS-14 | save-path | high | ⛔attended | `gaDeleteClient`/`gaSetArchived` don't touch cms → deleted/archived clients still inflate the portfolio trend chart (RPC has no deleted_at/archived filter). |
| ISS-15 | save-path | med | ⛔attended | cms upsert failure is caught-and-logged after the blob write → time-series silently diverges; green "saved" toast still fires. |
| ISS-16 | save-path | med | ⛔attended | `importMultiple` ignores `gaSaveClient` result; an imported blob whose id collides with an existing `local_id` silently OVERWRITES that client (mig preserves incoming id). |
| ISS-17 | save-path | med | ⛔attended | Client bootstrap mints a fresh self-blob when `gaLoadClients` returns null (load ERROR, not empty) → duplicate self row, real data stranded. |
| ISS-18 | money / scale | med | ⛔attended | `monthlyRows` net_worth = savings(liquid)−debt ≠ canonical totalA−totalL (§3) → monthly net-worth trend understated/negative for asset-rich clients. Needs formula fix + backfill. |
| ISS-19 | security | high | 🟡owner | `api/_sanitize` allow-list filters TOP-LEVEL keys only; nested free-text (e.g. `customAssets[].desc`) passes verbatim to the public portal + linked clients. Owner: which nested fields are private? |
| ISS-20 | security | med | ⛔attended | `api/admin-members` grant/revoke authorized for ANY advisor (only `list` is admin-gated) → any self-registered advisor can comp Premium. Clean fix (gate behind `isAdmin`) but it's a permission change → attended, not autonomous. |
| ISS-21 | security | high | 🟡owner | Admin `list` gated by MUTABLE auth email, not a stable uid → email-change privilege-escalation if Supabase email-confirm is off. Fix: gate by uid (needs the real admin uids). |
| ISS-22 | security | med | 🟡owner | `resolve-portal` rate-limit is fail-open when Upstash unset (by design). Defense-in-depth only — token entropy makes brute force impractical. |
| ISS-23 | security | med | ⛔attended | `link.js` island snapshot uses `.limit(1)` with no order → arbitrary row frozen if a client account ever has >1 `clients` row (rare; client-role normally has one). |
| ISS-24 | product | med | 🟡owner | Signup lets anyone self-select "Advisor"; advisors are never Premium-gated → Free/Premium model bypassable at registration (D-13b territory). |
| ISS-25 | product | med | 🟡owner | PremiumUpgrade "I already subscribed — activate" flips `accountPlan` client-side with no server verify (known honor-system; tighten now that the webhook exists?). |
| ISS-26 | api / billing | med | 🟡owner | `stripe-webhook` grants premium from `client_reference_id` without checking `payment_status==='paid'`/`mode`, and no event de-dup → premium before capture / on replay. |
| ISS-27 | api / scale | 🟢 | `patchByEmail` listed only first 200 auth users → grant/revoke silently no-op'd past page 1. **FIXED** v0.83.11 (paginated like `loadClients`). |
| ISS-28 | calc | 🟢 | `HomeEquityCalc` "Months Saved" rounded payoff up to whole years → understated/0/negative. **FIXED** v0.83.9. |
| ISS-29 | calc | 🟢 | `HomeEquityCalc` "Interest Saved" was a fabricated approx unrelated to the amort table. **FIXED** v0.83.9. |
| ISS-30 | i18n (D-3) | 🟢 | `AmortTablePaginated` hardcoded English headers + "Yr". **FIXED** v0.83.10 (passed `t`, new keys). |
| ISS-31 | i18n (D-3) | 🟢 | `EquityTablePaginated` hardcoded English headers + "Yr". **FIXED** v0.83.10. |
| ISS-32 | i18n (D-3) | 🟢 | `clientCalcs` 5 "Prefilled from…" helper lines hardcoded. **FIXED** v0.83.10 ({n}-token replace). |
| ISS-33 | i18n (D-3) | 🟢 | `clientCalcs` "HOUSEHOLD COMBINED" block hardcoded. **FIXED** v0.83.10 (reused income keys + new header key). |

## Recently fixed (recurrence-watch — prune after a few clean cycles)

| ID | Area | Status | One-line | Shipped |
|---|---|---|---|---|
| ISS-86 | a11y (WCAG 1.3.5) | 🟢 | **3 user-self-entry inputs collecting the user's own contact info lacked `autoComplete`** → no programmatic input-purpose (WCAG 2.1 SC 1.3.5, Level AA) + no browser/password-manager autofill on the primary signup path: (1) the landing **hero email-capture** (`pages/landing.jsx:647`, top-of-funnel, value→`ga_signup_email`→signup) had `type="email" inputMode="email"` but no `autoComplete`, while the sibling **login** email on the same page (`:213`) already carries `autoComplete="email"`; (2/3) the **public intake form's own email+phone** (`pages/intake.jsx:456/457`) — the line-450 note confirms these are the submitter's *own* details ("your advisor pre-filled your name, email, and phone"), and the parallel prospect form (`:600/:618`) already uses `tel`. Added `autoComplete="email"`/`"tel"` accordingly. **Deliberately NOT touched:** the advisor "Prospect"/"Partner" invite-form emails (`intake.jsx:595/613`) collect data *about a prospect*, not the filling user, so autofilling the filler's own email would be wrong (SC 1.3.5 scopes to the user's own info); SSN inputs keep their intentional `autoComplete="off"`+`data-1p-ignore`. **Zero new keys** (`autoComplete` values are HTML tokens, not visible strings → EN/ES symmetry unchanged). **Pure additive DOM attribute, no behavior/logic/save-path change → autonomous-safe push (matches the ISS-75/83/85 a11y-attribute class).** Gates: build clean (545ms, only ISS-07 chunk warn); lint 320=baseline (301 err/19 warn, 0 new); EN/ES unchanged. Found in the 2026-06-27 item-4 objective-a11y input-purpose sweep (after verifying `target="_blank" rel` + `<img alt>` classes both already clean). | v0.83.52 (2026-06-27) |
| ISS-85 | a11y | 🟢 | **5 icon-only `×` remove buttons had no accessible name** (no `aria-label`, no `title`) — `clientCalcs.jsx:145` (remove debt scenario), `clientModals.jsx:14` (remove card promo), `clientReports.jsx:269` (remove portfolio holding), `:286` (remove main-pack holding), `:290` (remove alt-pack holding). A screen reader announced only the bare glyph ("times"), giving SR users no idea which row's delete control they were on. The ISS-72/ISS-83 sweeps covered *close* buttons (`Modal`/`IAdd`/intake/admin) but never these *remove-row* controls. Added `aria-label`+`title` = `` `${t?.removeSvc||"Remove"} ${item}` `` where `item` is the row's own already-rendered name (`d.name`/`p.label`/`tOf(h)`/`h.ticker`/`s.ticker`) — so each control speaks e.g. "Remove VOO" / "Quitar VOO". **Zero new keys** — reuses the existing `removeSvc` key (`"Remove"`/`"Quitar"`), so the ES label renders and EN/ES symmetry is unchanged. **Pure additive DOM attribute, no behavior/save-path/logic change → autonomous-safe push (matches the ISS-72/83 a11y class).** Gates: build clean (3.35s); lint 408=baseline (0 new in the 3 edited files); EN/ES unchanged. Found in the 2026-06-27 item-4 objective-a11y close/remove-glyph sweep (traced every `×`/`✕` `<button>` in `src`: all close glyphs already labeled post-ISS-83; these 5 remove glyphs were the lone unlabeled set). | v0.83.51 (2026-06-27) |
| ISS-84 | pdf / money / display | 🟢 | **The emailed Complete/Financial report's Assets line-item table (`api/render-report-pdf.js`, `buildPrintHTML`) omitted `marketInvestments` rows while its Total included them.** The table mapped `accountsRows`+`customRows` only — never `miRows` — but the Total row used `agg.totalAssets`, which counts MI (`computeAggregates:164-165`; canonical `totalA` includes MI per `golden-anchor-logic §3`). So an MI-holding client's PDF assets table line items summed to `totalAssets−ΣMI` while the printed Total showed full `totalAssets` (didn't reconcile), and the holdings were invisible in the breakdown — even though the Asset-Map treemap right above listed them and the in-app balance sheet (`FinancialStatementsTab`, `clientReports.jsx:191`) renders MI as line rows. Same MI-omission class as ISS-47/50/53, in the emailed deliverable. Fix: added an `miRows` row map after `customRows` (label `📈 {ticker} {name}`, type=`a.cat`/"Investment"/"Inversión", value=`a.value`) + widened the render gate to include `miRows.length` (MI-only client now gets the table). **No new keys** (inline `isEs` ternary + stored data values). **Server-side display-only — builds HTML from already-saved data, no DB write / not the save path → autonomous-safe push.** Headlessly verified via the exported pure `buildPrintHTML` (node harness: MI rows present EN+ES, line items + $395,000 total reconcile, MI-only client renders). Gates: build clean (618ms); lint 3=pre-existing baseline (720/734/1214, 0 new); EN/ES unchanged. Found in the 2026-06-27 item-1 fresh correctness sweep of the never-money-traced PDF render path (`computeAggregates` totals + `effectiveMin`/loan-min + the treemap all verified canonical; the assets *table* was the lone omission). | v0.83.50 (2026-06-27) |
| ISS-83 | a11y | 🟢 | **The 2 Members-admin `×` close buttons (`pages/admin.jsx:53` avatar-picker header, `:461` settings-editor header) had `title` only, no `aria-label`** — the lone icon-only controls the 2026-06-26 a11y sweep (ISS-72, v0.83.40, which covered `Modal`/`IAdd` in `primitives.jsx` + `intake.jsx`) missed. `title` is an unreliable accessible-name source on touch/mobile screen readers (no hover, inconsistent AT), so SR users on those two modals heard no spoken label. Added `aria-label={t?.close\|\|"Close"}` next to the existing `title` on both. **Zero new keys** — reuses the existing `close` key (`"Close"`/`"Cerrar"`), so the ES label renders correctly and EN/ES symmetry is unchanged (2116/2116). **Pure additive DOM attribute, no behavior/save-path/logic change → autonomous-safe push (matches the ISS-72 a11y class).** Gates: build clean (682ms); lint 408=baseline (0 new); EN/ES 2116/2116. Found in the 2026-06-27 item-4 objective-a11y icon-button accessible-name sweep (`aria-label` count across `src` was 1 → traced every icon-only `<button>`; admin.jsx was the only title-only outlier). | v0.83.49 (2026-06-27) |
| ISS-82b | i18n (D-3) | 🟢 | **ISS-82 leftover closed — the 2 reused-name keys split into distinct per-meaning keys.** ISS-82 excluded `liquidAssets` ("Liquid Savings" client KPI vs "Liquid Assets" advisor KPI) and `noClientsYet` (4 distinct empty-state strings) because one shared key would change EN render at some call sites. Split each: `liquidAssets`→`liquidSavingsLbl`(`App.jsx`)+`liquidAssetsLbl`(`dashboard.jsx`); `noClientsYet`→`noClientsYet`("No clients yet." ×4 sites)+`noClientsYetDonut`("No clients yet")+`addClientsToPopulate`+`noClientsShort`. Every EN value = the existing fallback **verbatim** (EN byte-identical) + proper ES → ES leak closed on these last spots. **+6 EN/ES keys** (2110→2116). **Pure display, every site keeps its English fallback, no logic/save-path → autonomous-safe (matches ISS-61–82 class).** The owner yes/no queued in CRUISE_QUESTIONS 2026-06-27 is now resolved autonomously — the EN-preserving split needed no product decision (rec had said YES, loop-ok). Gates: build clean (592ms); lint 408=baseline (0 new); EN/ES 2116/2116 (0 asym/empty); 0 remaining `t.liquidAssets` refs. | v0.83.48 (2026-06-27) |
| ISS-82 | i18n (D-3) | 🟢 | **38 referenced translation keys had NO definition in `src/translations.js` → for Spanish users `t.KEY` resolved `undefined` and every site silently fell through its `\|\|"English fallback"`, so ES advisors/clients saw English text on the advisor dashboard, the tax/paycheck calculator, the intake form, the profile/settings modal, client reports, the promo card, and the landing theme toggle.** A NEW detection class the prior hardcoded-JSX-text sweeps (ISS-30–33/55/57/58/61–79) structurally COULD NOT catch: the strings *are* wired through `t.KEY` (look localized), but the key was never added — the `\|\|fallback` masks the miss in EN and leaks English in ES. Found via a key-integrity cross-reference (every `t.KEY`/`gaLabel("KEY")` ref vs the `T.en`/`T.es` key sets). Added all 38 to BOTH dicts, EN value = the existing fallback **verbatim** (EN render byte-identical, purely additive) + a proper ES translation: dashboard (`combinedDebt`/`combinedNetMo`/`checkingSavingsLbl`/`incomeVsSpendingHdr`/`spending`/`netLbl`/`netWorthDistributionHdr`/`netWorthDistributionSub`/`totalNet`/`tierHigh`/`tierMid`/`tierLow`/`tierNeg`), tax calc (`effectiveTaxRateHdr`/`paycheckBreakdownHdr`/`homeValueSplitHdr`/`grossLbl`/`netSlice`/`preTaxSlice`/`fedTaxSlice`/`stateTaxSlice`/`ssTaxSlice`/`medTaxSlice`/`taxRateLbl`/`lockedEquity`/`lowerIsBetter`), intake (`goalsAndNotesHdr`/`generalNotesLbl`/`shortTermLbl`/`midTermLbl`/`longTermLbl`), reports (`cashFlowWaterfallLbl`/`totalOutLbl`), profile modal (`customColorLbl`/`lastBackupLbl`), promo (`promoExpired`), landing (`switchToDark`/`switchToLight`). Currency-range tier labels (`$250K+`/`$50K–250K`/`$0–50K`) are language-neutral so EN=ES by design. **2 keys deliberately EXCLUDED** — `liquidAssets` ("Liquid Savings" vs "Liquid Assets") and `noClientsYet` ("No clients yet"/"Add clients to populate."/"No clients."/"No clients yet.") reuse ONE name across distinct strings, so a shared key would change EN at some call sites (not additive) → needs a call-site split, queued in CRUISE_QUESTIONS 2026-06-27 (Rec: distinct keys, attended). `t.pricing`/`t.resourcesHeadline` left as-is — their fallbacks are already lang-aware (`lang==="es"?…`). **Pure-data translations.js insert + a build-marker bump — no logic/JSX/save-path touch, every call site keeps its English fallback → autonomous-safe push (matches the ISS-61–79 i18n class).** Gates: build clean (790ms); lint 408 err = baseline (translations.js not flagged, 0 new); EN/ES **2110/2110** (was 2072/2072, +38 each, 0 asym, 0 empty); integrity re-scan: 0 dangling-with-fallback remaining (only the 2 excluded multi-fallback keys + recharts/Date false-positives). Found in the 2026-06-27 item-1/4 translation-key-integrity cross-reference scan (a detection method not previously run). | v0.83.47 (2026-06-27) |
| ISS-79 | i18n (D-3) | 🟢 | **The advisor Investments tab portfolio-model editor (`InvestmentsTab`, `src/components/clientReports.jsx` — the Portfolio / Main Packages / Alternative Packages views, a Premium advisor surface) rendered ~20 hardcoded-English UI strings regardless of language** while the rest of the report surface is bilingual → a Spanish advisor saw an English portfolio editor. Localized all three views: Portfolio (`💹 Portfolio` hdr; `🗑 Remove from Report`/`📌 Include in Report`/`📥 Export Holdings → Market Investments` buttons; per-rate `Save`; `Expected annual return`; `⚠️ Over 100%`/`Holdings {pct}%` status; `Show Main`/`Show Alt` toggle; `Name and description are pulled automatically…` note; inner `Alternative Packages` sub-hdr; `Use Package`/`Add`; the 💡 PROJECTION panel — `From savings allocation:`, `Stocks:`, `Other:`, `Contributed`/`Growth`/`Future Value ({years}yr @ {ret}%)` rows) and Main/Alt Packages (two 💡 help blurbs; `＋ Add Holding`/`Cancel`; `Use Package`, `Add to Package`, `Add to Client`). Same class as ISS-30–33/55/57/58/61–78. **+13 new EN/ES keys** + heavy reuse of pre-staged-but-unwired keys (`usePackage`/`addToClient`/`addToPackage`/`addHolding`/`removeFromReport`/`includeInReport`/`contributed`/`portfolio`/`alternativePackagesLbl`/`exportHoldingsTitle`/`growth`/`save`/`cancel`/`yrAbbr` — the ISS-71 "wire the existing key" pattern). `{port.risk} Risk` left as-is (constant data label, like ticker/pack names — consistent with prior sweeps). **Pure display — portfolio math (FV/alloc/payoff), `onUpdate` payloads, `saveRates`/`useP`/`useMain`/`addTo*Pack` logic + stored values untouched; English fallback on every string → not save path → autonomous-safe push (matches ISS-71/76/77/78).** Gates: build clean (445ms); lint 427/408 = baseline (0 new); EN/ES 2072/2072 (0 asym). Found in the item-1/4 advisor-surface hardcoded-JSX-text i18n re-scan (`clientReports.jsx` InvestmentsTab). | v0.83.46 (2026-06-27) |
| ISS-78 | i18n (D-3) | 🟢 | **The Promotions create/edit form (`src/pages/marketing.jsx`, advisor surface — present twice: inline-edit row + "＋ New Promotion" card) rendered its always-visible discount-value `<Label>` and two input placeholders in hardcoded English regardless of language.** The ISS-71 sweep localized the *suggested-promotions examples* but missed this interactive form. (1) `:116`/`:166` the dynamic value label `draft.type==="percent"?"% Off":draft.type==="flat"?"$ Off":"Bundle Price ($)"` — the genuine violation, an always-rendered form label, not a disappearing hint; (2) `:111`/`:161` promo-name placeholder `'e.g. "New Year Reset 2026"'`; (3) `:112`/`:162` promo-code placeholder `"e.g. WELCOME25"`. Same class as ISS-30–33/55/57/58/61–77. Label fix is **zero-new-key** — reuses `percentOff`("% Off"/"% Descuento") / `flatOff`("Flat $ Off"/"Descuento Fijo $") / `bundlePrice`("Bundle Price"/"Precio del Paquete")+" ($)". **+2 new EN/ES keys** (`promoNamePh`/`promoCodePh`) for the placeholders; each site keeps an English fallback. **Pure display — promo `draft` state, `save`/`del`/`toggleActive` payloads, `onSave` untouched → not save path → autonomous-safe push (matches ISS-71/72/73/75/76/77).** Gates: build clean (538ms); lint 427/408 = baseline (0 new); new keys present EN+ES (×2 each). Found in the item-1/4 advisor-form placeholder/label i18n re-scan. | v0.83.45 (2026-06-27) |
| ISS-77 | i18n (D-3) | 🟢 | **Five user-visible strings in `src/components/charts.jsx` rendered hardcoded English regardless of language** — four chart empty/placeholder states + one forecast-cone axis label, the chart-component surface the prior aria-label sweep (ISS-65) didn't cover for *text nodes*: (1) `:81` `Waterfall` empty state (`segs.length===0`) "No data"; (2) `:143` `DebtSavings` empty state (`pts<1`) "No data"; (3) `:947` `AmortChart` placeholder (`tw<2`) "Adjust values"; (4) `:1007` `CompoundGrowthStack` placeholder (`series<2`) "Adjust inputs to see growth"; (5) `:1230` `ForecastCone` SVG `<text>` axis tick "Now". Same class as ISS-30–33/55/57/58/61–76. Wired via the file's existing local **`gaLabel(key,fallback)`** helper (resolves from `document.documentElement.lang`, imports `T` from the pure-data translations module — the ISS-65 pattern; defined locally NOT imported from `primitives.jsx` to avoid the `primitives→charts` circular dep). **Note the existing `noData` key was UNUSABLE** — it holds the longer message "No historical data yet."/"Sin historial." (a different surface), so a same-named key would be a `no-dupe-keys` error → created distinct `chartNoData`. **+3 new EN/ES keys** (`chartNoData`/`chartAdjustValues`/`chartAdjustInputs`) + 1 reused (`durNow` = "Now"/"Ahora"). **Pure display — no chart data/geometry/tween/callbacks/save path → autonomous-safe push (matches ISS-65/72/73/75/76).** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 2057/2057. Found in the item-1/4 `charts.jsx` hardcoded-JSX-text i18n re-scan. | v0.83.44 (2026-06-27) |
| ISS-76 | i18n (D-3) | 🟢 | **The Members admin page (`src/pages/members.jsx`, owner master view — accounts/plan counts/Stripe MRR/comp-Premium grant·revoke, nav-visible to admin advisors only) localizes via the inline `es?…:…` idiom and was almost fully bilingual, but TWO strings rendered hardcoded English regardless of language**: (1) `:21` the load-error fallback `r.error\|\|"load failed"` (shown in `state.error` `:43` when `list` returns `!ok` with no server `.error`); (2) `:37` the plan-chip comp suffix `{m.comped?" · comp":""}` (shown on every complimentary-Premium row). Same class as ISS-30–33/55/57/58/61–75 (hardcoded-EN in `es?`-idiom pages). Fixed: `:21` → `document.documentElement.lang==="es"?"Carga fallida":"Load failed"` (the `<html lang>` footgun pattern — NOT closing `load` over the reactive `es`, which made `react-hooks/exhaustive-deps` flag `useEffect(…,[])` for a missing `load` dep, +1 warning); `:37` → `es?" · cortesía":" · comp"` (matches the page's ES "Cortesía"/"Premium de cortesía" wording). `planChip` (`:37`) is not in a dep array → `es?` stays clean there. **Zero new keys** — inline-ternary idiom → EN/ES symmetry unchanged by construction (no `translations.js` edit). **Pure display — `gaAdminMembers`/`load`/`act`/grant·revoke logic + `state`/`grantEmail` payloads untouched → not save path → autonomous-safe push (matches ISS-72/75).** Gates: build clean; lint 427/408 = baseline (0 new errors, 0 new warnings — DOM-lang read avoids the exhaustive-deps regression); EN/ES symmetry unchanged. Found in the item-1 deepening scan of the last `es?`-idiom page not yet i18n-swept. | v0.83.43 (2026-06-27) |
| ISS-75 | a11y (WCAG 4.1.2/3.3.2) / i18n (D-3) | 🟢 | **The Useful-Links directory search `<input>` (`src/pages/links.jsx:33`, Premium-gated "help in every life situation" page) had a `placeholder` but NO `aria-label`** → its only accessible name was the placeholder (not an accessible name per WCAG; a screen reader announces an unlabeled edit field, and the hint disappears on first keystroke). The lone search-box gap: dashboard's two alert/due searches, the client-list search, and the three clientData modal searches all already carry `aria-label` from the prior a11y sweep. Same a11y-name class as ISS-41/65/72/73. Added `aria-label={es?"Buscar enlaces útiles":"Search useful links"}`. **Zero new keys** — `links.jsx` localizes via the inline `es?…:…` ternary idiom (public-style page, no `t.` keys; same as `landing.jsx` in ISS-72) → EN/ES symmetry unchanged by construction. **Pure a11y attribute — search `value`/`onChange`/`match` filter + all data untouched → not save path → autonomous-safe push (matches ISS-41/65/72/73).** Gates: build clean; lint 427/408 = baseline (0 new, no new `no-undef` — `es` in scope at `:13`); EN/ES symmetry unchanged. Found in the item-1/4 standalone-search-input (placeholder-only-label) a11y re-scan. | v0.83.42 (2026-06-27) |
| ISS-74 | i18n (D-3) | 🟢 | **The CC-vs-Loan calculator's "New Hypothetical Debt" scenario block (`components/clientCalcs.jsx`) rendered hardcoded English** while the rest of the same component used `t.x\|\|"fallback"` — Spanish users saw a localized calculator with an English scenario sub-form. Wired 6 strings: the **DEBTS TO INCLUDE** header (`debtsToInclude`), the **＋ Scenario/Cancel** toggle (`addScenarioBtn`+reused `cancel`), the **e.g. New Card** placeholder (`egNewCard`), the form **Cancel/Add Scenario** buttons (reused `cancel`+pre-existing `addScenario`), the **· scenario** row tag (`scenarioTag`), and the unnamed-debt fallback **Card/Loan** (reused `card`/`loan`). Same class as ISS-55/57/58/61–73. **+4 new EN/ES keys** (`addScenario` already existed — first insert dup'd it, `no-dupe-keys` caught it, dup removed; `＋ Escenario` matches the ES `prefillCardsLoans` text). **Pure display — scenario math (`ccMin`/`loanMin`/APR weighting), `newScen` state, `addScen`/`delScen` payloads untouched → not save path → autonomous-safe push (matches ISS-55/57/58/61–73).** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 2054/2054. Found in the item-1/4 advisor-surface hardcoded-JSX-text i18n re-scan (`clientCalcs.jsx`). | v0.83.41 (2026-06-27) |
| ISS-73 | a11y (WCAG 4.1.2) / i18n (D-3) | 🟢 | **The two icon-only tab-scroll arrows in `ClientDetail` (`src/App.jsx`) — the `‹`/`›` buttons that scroll the overflowing client-detail tab row — carried a hardcoded-English `title` (`"Scroll left"`/`"Scroll right"`) and NO `aria-label`** → their only accessible name was the bare glyph, and the tooltip stayed English in Spanish. Same class as ISS-41/65/72 (icon-only-control a11y sweep). Wired both `title` AND `aria-label` from `t.scrollTabsLeft`/`t.scrollTabsRight` (English fallback; `ClientDetail` already receives `t`). **+2 new EN/ES keys** (`scrollTabsLeft`/`scrollTabsRight`). **Pure display/a11y attribute — no scroll behavior/data/callbacks/save path → autonomous-safe push (matches ISS-41/65/72).** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 2050/2050. Found in the item-1/4 hardcoded-`title`/`aria-label` attribute re-scan. | v0.83.40 (2026-06-27) |
| ISS-72 | a11y (WCAG 4.1.2) / i18n (D-3) | 🟢 | **Five icon-only `<button>`s exposed a hardcoded-English `aria-label`** (the accessible name a screen reader announces) while their visible glyphs were already bilingual — so in Spanish the control was *named* in English. `marketing.jsx`: `ResourcesPage` guides carousel + `PricingCarousel` prev/next arrows (`"Previous"/"Next"`); `landing.jsx`: public-landing header language toggle (`"Toggle language"`) + footer theme toggle (`"Toggle theme"`). Same class as ISS-41 (`×` close buttons) / ISS-65 (chart SVG names) — completes the icon-only-control a11y sweep. Wired to each component's in-scope language var (`_gaLang()` in ResourcesPage / `L` in PricingCarousel / `lang` in landing header / `es` in landing footer) → `"Anterior"/"Siguiente"`, `"Cambiar idioma"`, `"Cambiar tema"`. **Zero new keys** — both files localize via inline ternaries (their established idiom; `landing.jsx` is the public page with its own inline i18n, not `t.`-keyed), so **EN/ES symmetry unchanged by construction** (no `translations.js` edit). **Pure display/a11y attribute — no data/geometry/callbacks/save path → autonomous-safe push (matches ISS-41/65).** Gates: build clean; lint 427/408 = baseline (0 new, no new `no-undef`); EN/ES symmetry unchanged. Found in the item-1/4 hardcoded-`aria-label` attribute scan. | v0.83.39 (2026-06-27) |
| ISS-71 | i18n (D-3) | 🟢 | **`PromotionsPage` "Suggested Starter Promotions" block (`src/pages/marketing.jsx`) rendered a localized header (`t.suggestedTitle`) over 6 hardcoded-English example-promo `<div>`s** — Spanish advisor saw a Spanish heading then 6 English bullets. Same class as ISS-30–33/55/57/58/61–70. **Zero new keys** — all six full-line strings already existed in BOTH T.en/T.es (`welcomeDiscountSugg`/`healthClientSugg`/`referralRewardSugg`/`newYearSugg`/`springSugg`/`annualBundleSugg`), pre-staged but never wired (`newYearSugg` was single-quoted → earlier dbl-quote scan missed it). Wired via `.map()` over the 6 keys (each `t.key\|\|"<EN fallback>"`), split on the em-dash (`indexOf(" — ")`) to keep the bold title (`• <b>{title}</b> — {body}`); en-dash date ranges in bodies stay intact (node-verified EN+ES). **Pure display — static suggestion examples, no promo data/`onSave`/save path → autonomous-safe push (matches ISS-61–70).** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES symmetry unchanged 2048/2048 (zero translations.js edits). Found in the item-1/4 advisor-surface hardcoded-JSX-text i18n scan. | v0.83.38 (2026-06-27) |
| ISS-70 | i18n (D-3) | 🟢 | **`ClientDetail` archive/restore **confirmation modal** (`src/App.jsx`, opened from the client-detail kebab → 📦 Archive / ↩ Unarchive) rendered fully hardcoded English regardless of language** — the last named sub-scope ISS-68/69 left queued ("ClientDetail archive/restore modal body text in App.jsx — a separate sweep"). Localized the title (`archiveModalRestoreTitle`/`archiveModalArchiveTitle`, emoji kept inline), the restore body (`archiveModalRestoreA`+`<b>{name}</b>`+`archiveModalRestoreB`, ES wraps bold inside `¿…?`), the archive body (`archiveModalArchiveA/B` "…? Data is preserved and can be restored."), Cancel (reused `cancel`), and the dynamic action button (Restore=`restoreBtn` / Archive=reused `kebabArchive`). Same class as ISS-30–33/55/57/58/61–69. **+7 new EN/ES keys** + 2 reused; English fallback on every string. **Pure display — `onArchive(client.id)` callback, the `client.archived` branch gate, and stored payloads untouched → not save path → autonomous-safe push (matches ISS-61/64/68/69).** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 2048/2048. **Completes the ISS-68/69 out-of-scope queue** — last hardcoded confirmation surface in that batch. Found in the item-1/4 advisor-surface i18n sweep. | v0.83.37 (2026-06-27) |
| ISS-69 | i18n (D-3) | 🟢 | **`ExportHoldingsModal` (`components/clientReports.jsx`) — the advisor "Export Holdings → Market Investments" modal — wired only its title + 4 keys; the modal body rendered hardcoded English regardless of language** (the exact out-of-scope item ISS-68 queued). Localized: the intro paragraph (split `expHoldIntroA` + bolded `marketInvestmentWord` + `expHoldIntroB` to keep the mid-sentence bold), the **TARGET MONTH** (`targetMonthLbl`) + **HOLDINGS** (`holdingsLbl`) labels, the **Select All**/**Clear All** buttons (reused `selectAllVisible`/`clearAll`), the "across {n} holding(s)" summary (`acrossHoldings` + `holdingWord`/`holdingsWord` for EN/ES-correct singular·plural — EN holding/holdings, ES tenencia/tenencias), and the dynamic "Add to {Profile\|month}" button (`addToLabel`+`profileWord`). Same class as ISS-30–33/55/57/58/61–68. **+10 new EN/ES keys** + 5 reused; ES aligns to existing `exportHoldingsTitle` ("Tenencias"/"Inversiones de Mercado"); global-token `.replace()` interpolation. **Pure display — `apply`/`onSave`/`exportHoldings` payloads, selection/amount state, the `current`/snapshot target value untouched → not save path → autonomous-safe push (matches ISS-61/64/68).** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 2041/2041; node interpolation harness clean. **Completes the ISS-68 out-of-scope queue (still remaining: `ClientDetail` archive/restore modal body text in App.jsx — a separate sweep).** Found in the item-1/4 advisor-surface i18n sweep. | v0.83.36 (2026-06-27) |
| ISS-68 | i18n (D-3) | 🟢 | **Every remaining hardcoded-English native `alert()`/`confirm()` browser dialog rendered English regardless of language** — Spanish advisors got English confirmation/notification popups while sibling dialogs were already localized (`profileModal`/`admin`/`portal`/`clientReports` tickerReq), so the surface was inconsistent. Localized all across 5 files: **clientReports.jsx** (`portfolioInclReport`, `confirmRemovePortfolio` ×2, `confirmRemoveCompare`, `confirmRemoveCalcs`, `selectOneMonth`, `appliedToMonths`, `addedHoldingsProfile`, `addedHoldingsMonth`, `selectOneHolding`); **clientCalcs.jsx** (reused `nameBalanceReq`; new `snapshotSavedMsg` — distinct from short `snapshotSaved` button label — `confirmClearSnap`, `confirmClearAllSnaps`); **admin.jsx** (reused `confirmRemoveSvc`; new `confirmDeleteClient`); **marketing.jsx** (reused `promoNameReq`; new `confirmDeletePromo`); **App.jsx** ClientDetail (`csvImported`, `invalidCsv`, `copyFailed` ×2). Same class as ISS-30–33/55/57/58/61–67. **+17 new EN/ES keys** + 3 reused; full-phrase `.replace()` token interpolation (`{n}`/`{m}`/`{x}`/`{name}`). **Pure display — every `confirm()` boolean gate, mutation payload (`onUpdate`/`onDelete`/`onSettingsChange`/`parseCSV`), and `alert()` trigger condition untouched; only displayed text changed, each with an English fallback → not save path → autonomous-safe push (matches ISS-61/64).** Gates: build clean; lint 427 (408 err) = baseline (0 new, 0 `no-undef`); EN/ES 2031/2031 (0 asym); node interpolation harness clean. Found in the item-1 cross-surface `alert`/`confirm` i18n scan. **Out-of-scope (queued for a later tick):** the surrounding modal *body* prose still hardcoded — `ExportHoldingsModal` labels (TARGET MONTH/HOLDINGS/Select All/Clear All/"Add to…"), `ClientDetail` archive/restore modal text — a distinct full-component sweep. | v0.83.35 (2026-06-27) |
| ISS-67 | i18n (D-3) | 🟢 | **`FinancialPlanTab` (`components/clientReports.jsx`) — the interactive advisor-editable "Plan" tab — was the named ISS-66 next-tick sub-scope.** It shared every Strategy-Plan string with the read-only `PlanReportBlock` (localized v0.83.33) but rendered them **hardcoded English regardless of language**: the live editing surface lagged the report/PDF it feeds. Localized all of it, reusing the 14 ISS-66 keys verbatim — `fmtDur`/`addDate` (now `_gaLang()`-locale), `phaseLbl`, `financialRoadmapHdr`, `debtFreeFocus`, `minPerMo`, all 3 `phaseNSub` + 3 `phaseNNoteDefault` (interpolated `{strat}`→localized `avalanche`/`snowball`), the INVESTMENT PROJECTION row (`startsLbl`/`ofExtraCash`/`fivePctYears`/`tenYears`/`twentyYears`/`growthSuffix`), `startNow` — plus the editable-UI bits the read-only block lacks (Avalanche/Snowball toggle + new short hints, ✏️ Edit/Cancel/Save buttons, empty-note prompts, ADDITIONAL NOTES header, CLIENT GOALS block). Same class as ISS-30–33/55/57/58/61–66. **+10 new EN/ES keys** + 14 reused. **Pure display — plan math (`calcPayoff`/`investFV`/`effectiveMin`) + `onUpdate` payloads (`planOverrides`/`planStrategy`/`notes`) untouched; localized strings are defaults/labels never persisted (drafts init from `override\|\|""`, goals save by key not label) → not save path → autonomous-safe push.** Gates: build clean; lint 408/19 = baseline (0 new); EN/ES 2014/2014. Found completing the ISS-66 sub-scope. | v0.83.34 (2026-06-27) |
| ISS-66 | i18n (D-3) | 🟢 | **`PlanReportBlock` (`components/reportBlocks.jsx`) — the Strategy Plan block in the Monthly/Financial-Statement/Full reports (ships in the client-facing PDF) — wired only KPI + strategy-caption keys; the whole roadmap body rendered hardcoded English regardless of language.** A Spanish client got an English strategy plan. Localized: the "Phase N" label (reused `phaseLbl`), the FINANCIAL ROADMAP header (reused `financialRoadmapHdr`), the 🎉 Debt Free callout, the per-debt "Min {x}/mo" line, all 3 phase subs + 3 phase note-defaults (coaching prose), the INVESTMENT PROJECTION row ("starts…/% of extra cash", 5/10/20-year labels via `fivePctYears`/`tenYears`/`twentyYears`, "growth"), the "Start now" badge, and the Additional Notes / Recommendations header. `fmtDur` ("Now"/"mo"/"yr") and `addDate` (hardcoded `en-US`→`_gaLang()` locale) now bilingual; interpolated `{strat}` resolves to localized `avalanche`/`snowball`. Same class as ISS-30–33/55/57/58/61–65. **+11 EN/ES keys** + 14 reused pre-staged keys; full-phrase `.replace()` token interpolation. **Pure display — no plan math (`calcPayoff`/`investFV`/`effectiveMin`), no stored payloads, no save path → autonomous-safe push.** node symmetry+interpolation harness clean (zero asymmetry, zero leftover tokens). Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 2004/2004. **Sub-scope DONE → ISS-67 (v0.83.34):** the interactive `FinancialPlanTab` (`clientReports.jsx`) reused these keys. Found in the item-1/4 report-surface i18n scan. | v0.83.33 (2026-06-26) |
| ISS-65 | a11y (WCAG 4.1.2) / i18n (D-3) | 🟢 | **Four chart SVGs in `components/charts.jsx` had hardcoded-English `aria-label`s** — `Waterfall` ("Cash flow waterfall"), `SmoothAreaLine` ("Trend line chart"), `RankedHBars` ("Ranked horizontal bars"), `Radar5` ("Radar chart") — the accessible name a screen reader announces for the `role="img"` SVG, so it stayed English in Spanish (a11y + D-3 gap). Plus `profileModal.jsx` two hardcoded `title` tooltips ("Stripe link set" / "No Stripe link"). Same a11y class as ISS-41 (icon-only buttons). **Footgun (ISS-41):** the four charts don't receive `t` → added a **local `gaLabel`** in `charts.jsx` resolving from `document.documentElement.lang` (same as `primitives.jsx`); defined locally, NOT imported, because `primitives.jsx`→`charts.jsx` already imports — re-importing `gaLabel` back would be a **circular dependency**. `charts.jsx` imports `T` from the pure-data translations module (no cycle). `profileModal` has `t` in scope → used `t.stripeLinkSet`/`t.noStripeLink`. **Pure display — no chart data/geometry/save path → autonomous-safe push.** +6 EN/ES keys. node resolver harness confirms EN/ES + fallbacks. Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1993/1993. Found in the item-4 a11y/i18n attribute scan. | v0.83.32 (2026-06-26) |
| ISS-64 | i18n (D-3) | 🟢 | **`ExportModal` (`components/clientData.jsx`) — advisor "Export Clients" modal — wired only title + search; all other body text hardcoded English.** Localized: the "Format" label + both format tiles (Full Backup .json / Profile CSV titles+descriptions), the "Which clients" label, "All Active"/"Select Clients" mode toggle, the All/None selection buttons (reused `impAll`/`impNone`), the "{n} selected" count, "Cancel" (reused `cancel`), and the dynamic action button (💾 Export Backup / 📄 Export CSV); busy state already used `preparingBackup`. **+11 EN/ES keys** (`exp*` family + `exportBackupBtn`/`exportCsvBtn`). **CSV column-header row (`Name,…,SSN,…`) deliberately stays English** — data-interchange format for re-import; translating would break round-trip. **Pure display — `doExport`, CSV row builder, the `c.social` (SSN) field, export payloads untouched → not save path → autonomous-safe push (matches ISS-61/62/63).** SSN guard (`golden-anchor-logic §2/§6`) consulted: advisor-side export of own client data (≠ portal, where SSN never leaves server) → no SSN handling changed. **Completes the `clientData.jsx` advisor-surface i18n sweep.** Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1987/1987; node interpolation harness clean. Found in the item-1/4 advisor-surface i18n scan. | v0.83.31 (2026-06-26) |
| ISS-63 | i18n (D-3) | 🟢 | **`ImportWizard` (`components/clientData.jsx`) — the advisor 6-step "Import Client Data" flow — wired only step titles + a few helper keys; ~30 hardcoded-English body strings across all steps rendered regardless of language.** Localized every step: choose (3 import-type tile titles+descriptions), upload ("Upload File(s)" title, EXCEL/CSV headers, "{n} months found", "{n} clients found", "Couple:"/"Single:", the cards·bills·income-streams unit line, "Back", "Skip CSV →"), names ("Names detected…", "Partner (detected in file)", "{name} Color" labels), cards (couple/single hint, "Joint" via reused `t.joint`), csv_pick ("Select ONE…"/"Select which…", All/None, "{s} of {n} selected"/"1 client selected"/"Select 1 client to link", action btn "Import {n} Client{ps}"/"Link & Continue →"/"Select 1 to link"), confirm (months/credit-cards/bills//mo-income units, "Profile linked:", the Excel-omission note, the duplicate-name warning split pre/post bold, "✅ Import Client"). Same class as ISS-30–33/55/57/58/61/62. **+42 EN/ES keys** (reused `back`/`continueArrow`/`joint`/`firstName`/`lastName`/`partnerFirst`/`partnerLast`/`whatImport`/`parsing…`/`clickSelect…`/`noClientsMatch`/`searchClientsPh`); global-token `.replace()` pluralization (`{n}`/`{s}`/`{ps}`). **Pure display — `doImport`/`parseWorkbook`/`mig`/`mk`/`onImport` payloads + the `'Unnamed'`/`'Client'` data fallbacks untouched → not save path → autonomous-safe push (matches ISS-57/58/61/62).** **Remaining sub-scope:** `ExportModal` (SSN CSV → needs `golden-anchor-logic` guard). Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1976/1976; node interpolation harness clean (no leftover tokens, EN/ES plurals correct). Found in the item-4 advisor-surface i18n scan. | v0.83.30 (2026-06-26) |
| ISS-62 | i18n (D-3) | 🟢 | **`BackupImportModal` (`components/clientData.jsx`) — advisor backup-restore flow — wired only its title + search placeholder; all other body text hardcoded English.** Localized: the invalid-backup `setErr`, the "N clients in backup" count + "From {date}" stamp + "Select .json backup file" prompt, the mode toggle (🔄 Restore (select clients) / ⚠️ Replace All), Select All / Clear buttons, per-client "⚑ exists — will update" / "new" badges, the "Nmo" suffix, the restore summary ("S of N selected · U will update · A will be added"), the "⚠️ Replace All will permanently delete…" warning, the "{name} · {n} months" replace rows, Cancel, and the dynamic action button (Replace All / Restore N Clients). Same class as ISS-30–33/55/57/58/61. **+12 EN/ES keys** (reused `cancel`/`clear`/`months`/`selectAllVisible`/`searchClientsPh`/`restoreBackupTitle`); global-token `.replace()` pluralization (`{ps}` driven by `count!==1`). **Pure display — restore/replace import logic, stored payloads, `onImport` calls untouched** → not save path → autonomous-safe push (matches ISS-57/58/61). **Remaining sub-scope (future ticks):** `ExportModal` (SSN CSV → needs `golden-anchor-logic` guard), `ImportWizard` steps. Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1934/1934; node interpolation harness clean. Found in the item-4 advisor-surface i18n scan. | v0.83.29 (2026-06-26) |
| ISS-61 | i18n (D-3) | 🟢 | **`components/clientData.jsx` advisor data-management modals wired titles via `t?.x` but rendered all body content hardcoded English.** This batch localized the two client-management **confirmation** modals: **DuplicateResolverModal** (duplicate-count summary, "POSSIBLE DUPLICATES" header, Incoming/Existing captions, `partner:` label, Merge/Skip/Import-as-New buttons, "N new clients will be imported.", Cancel/Apply) and **DeleteClientModal** ("This action is permanent", the {name}+{n}-months delete warning, Cancel, Delete Forever). Same class as ISS-30–33/55/57/58. +14 EN/ES keys (reused `cancel`); global-token `.replace()` pluralization (`{ps}`/`{ms}`/`{pe}`/`{mv}` driven by `count!==1`) keeps one key/language correct singular+plural; `<b>{name}</b>` emphasis preserved via pre/post fragments (`deleteClientWarnA`/`B`). **Pure display — stored payloads + delete/merge/save logic untouched** → autonomous-safe push (matches ISS-57/58). **Remaining sub-scope (future ticks):** `BackupImportModal`, `ExportModal` (SSN CSV → needs `golden-anchor-logic` guard), `ImportWizard` steps. Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1922/1922; node interpolation harness clean (no leftover tokens, EN/ES plurals correct). Found in the item-4 advisor-surface i18n scan. | v0.83.28 (2026-06-26) |
| ISS-59 | report / money / display | 🟢 | **`components/clientReports.jsx` itemized the per-card minimum from raw `c.min` in two report surfaces while their own footer used canonical `sumMin`** (= Σ `effectiveMin`), so the breakdown rows didn't reconcile to the total directly below. (1) **CashFlowStatement** (`:156`) "DEBT SERVICE" list filtered `c.min>0` + showed `value={c.min}` above "Total Debt Service"=`minD`; (2) **FullReport** (`:307`) per-card "Min Pay" column = `fmtD(c.min)` vs footer `fmtD(minD)`. Same class as ISS-44/46/48/50/56 — here in the report breakdown surfaces. Four modes: balance w/ unset min → row hidden but in total; paid-off card w/ stale min → phantom $0-balance row; below-$25-floor shown raw; min>balance uncapped. Fix: both use `effectiveMin(c)` for filter + value. | **Pure display, not save path** (NMModal/monthlyRows use `sumMin`; raw-min snapshot save is the separate ⛔ ISS-48) → autonomous-safe push (matches ISS-56). node harness `scratchpad/iss_cfmin.mjs` (4 cases): OLD rows $205 ≠ $230 footer → NEW $230 = footer. Gates: build clean; lint 427/408 = baseline (0 new); no new strings. Found in the item-1 `clientReports.jsx` raw-`.min` correctness scan. | v0.83.27 (2026-06-26) |
| ISS-58 | i18n (D-3) | 🟢 | **`components/clientModals.jsx` had ~30 hardcoded-English strings beyond the ISS-57 dropdowns** — all six data-entry modals (Card/Account/Loan/Bill/Asset/Split/Join + Income footer) rendered English regardless of language. Modal **edit-titles** (`${t.editLabel} Card/Account/Loan/Bill/Asset` → now `${t.editLabel} ${t.card/…}`, mirroring `IncomeModal`); CardModal's 4 promo `setErr` + save over-balance errors, "Suggested:", "Limit ($)", "(1-31, optional)", "e.g. 15"/"e.g. Balance Transfer" placeholders, "No promotional rates…", "at … % APR · ends …", "Add"; IncomeModal "Annual:"; AccountModal "e.g. Chase Checking" + liquid/investment/household captions; LoanModal "e.g. Honda Accord"; AssetModal "Equity:"/"Gain:"; SplitModal auto-assign note + "Day"/"items"; JoinModal "becomes Person 1…" + "Select a client first." alert. Same class as ISS-30–33 / ISS-55 / ISS-57. | **Pure display — stored `onSave` payloads, the `np.label\|\|"Promo"` stored default, and `/mo` untouched → not save path → autonomous-safe push (matches ISS-57).** +27 new EN/ES keys (reused `editLabel`/`annual`/`dayLbl`/`limitField`; the last two pre-existed → initial insert duplicated them, `no-dupe-keys` caught it, dupes removed). Gates: build clean; lint 427/408 = baseline (0 new); EN/ES 1908/1908. Found in the item-1 `clientModals.jsx` i18n scan. | v0.83.26 (2026-06-26) |
| ISS-57 | i18n (D-3) | 🟢 | **`CardModal` / `AccountModal` / `LoanModal` (`components/clientModals.jsx`) hardcoded the person/owner `<select>` labels** — `[["joint","Joint"],["p1",client?.firstName\|\|"Person 1"],["p2",client?.partnerFirst\|\|"Person 2"]]` — so the "Owed by"/"Owner" dropdown showed **English regardless of language**, while the sibling `IncomeModal`/`BillModal` already wire `t.joint`/`t.p1`/`t.p2` (same data-entry surface, inconsistent). Also `CardModal.save` raised a bare `setErr("Name required.")` vs siblings' `t.nameReqErr`. Same class as ISS-30–33 / ISS-55 (hardcoded English in extracted components). Fix: all three dropdowns → `t?.joint\|\|"Joint"` / `client?.firstName\|\|t?.p1\|\|"Person 1"` / `client?.partnerFirst\|\|t?.p2\|\|"Person 2"`; `CardModal` error reuses `t.nameReqErr`. **Pure display — stored `owedBy`/`owner` values unchanged → not save path → autonomous-safe push. Zero new keys** (reused `joint`/`p1`/`p2`/`nameReqErr`, present in both T.en/T.es) → EN/ES symmetry unchanged by construction. Gates: build clean; lint 427/408 = baseline (0 new). Found in the item-1 `clientModals.jsx` i18n scan. | v0.83.25 (2026-06-26) |
| ISS-56 | reminders / money / display | 🟢 | **`getClientRem` (`utils/finance.js:87`) — the client "Card Min" reminder — gated and displayed from raw `cc.min`** (`if(cc.min>0)` … `amount:cc.min`) instead of canonical `effectiveMin(cc)` (§3). The advisor path (`getAdvRem` highDSR) already uses `sumMin`/`effectiveMin`, so the two reminder surfaces diverged on identical data. Four failure modes: (1) card with balance but unset `min` → real reminder **hidden**; (2) paid-off card (`balance:0`) with stale `min` → **phantom** reminder for $0 balance; (3) below-$25-floor min shown raw; (4) min > balance shown raw (not capped). Same class as ISS-45/46/48/50, here in the client reminder surface. Fix: `const em=effectiveMin(cc); if(em>0) … amount:em`. **Pure display, not save path** (dismissal key `cardDue:{cid}:{cardId}:{YYYY-MM}` is amount-independent) → autonomous-safe push (disposition matches ISS-44). node harness `scratchpad/iss56.mjs` (5 cases) proves all four. Gates: build clean; lint 427/408 = baseline (0 new); no new strings. Found in the item-1 `utils/finance.js` reminder-engine correctness scan. | v0.83.24 (2026-06-26) |
| ISS-55 | i18n (D-3) | 🟢 | **`MarketInvestmentsSection` + `MarketInvestmentModal` (`clientSections.jsx`) had hardcoded English** — modal title (Edit/Add Market Investment), "＋ Add Investment", empty state "No market investments yet…", footer "Value:"/"Gain/Loss:", and the inline "{n} shares" unit all rendered EN-only regardless of language (D-3 violation). Same class as ISS-30–33 (calculator hardcoded English). The save alert "Ticker and name required." was hardcoded **despite `t.tickerNameReq` already existing**. Fix: 6 display edits wiring `T`; +7 EN/ES keys (`editMarketInv`/`addMarketInv`/`addInvestment`/`noMarketInv`/`valueColon`/`gainLoss`/`sharesUnit`), alert reuses `tickerNameReq`. Pure display, not save path → autonomous-safe push. Gates: build clean; lint no new (408 err); EN/ES 1881/1881. Found in the item-1 `clientSections.jsx` i18n scan. | v0.83.23 (2026-06-26) |
| ISS-54 | portal / money / display | 🟢 | **`pages/portal.jsx` share-portal overview AND `LinkedOverview` double-count property** in the asset-allocation donut: both compute the "Property & assets" slice `propV` as `[].concat(c.customAssets,c.properties)` (the **union**) but `customAssets`/`properties` are mutually-exclusive aliases — `getProperties(c)` (`finance.js:59`) chooses ONE and canonical `totalA` counts them **once, never both** (`golden-anchor-logic §6`). Donut center = `totalA(c)`, so unioning both makes the slices overstate property + not reconcile to the center whenever `c.properties` is non-empty (ISS-49-class legacy blobs → ~2× the property value; would become universal for any custom-asset client once the ISS-49 `mig` fix lands). Same root as ISS-49 (the alias) but a distinct, independent **display** double-count — **not** the save path → autonomous-safe push. Fix: both `propV` use canonical `getProperties(c)`. node harness `scratchpad/iss54.mjs`: legacy $200k home slices $480k→$280k (=center); fresh client unchanged. Found in the item-1 portal/LinkedOverview correctness scan (2026-06-26). | v0.83.22 (2026-06-26) |
| ISS-52 | export / money | 🟢 | The **AI client export** (`utils/aiExport.js`, `gaClientAIText`) never listed `marketInvestments`: its "Investments"/"Physical assets"/"Portfolio model" sections keyed on `c.investments`/`c.physicalAssets`/`c.portfolio` — **non-existent fields** (canonical = `marketInvestments`/`customAssets`/`savedPortfolio`, `golden-anchor-logic §6`), so they never fired. MI value IS in canonical `totalA` (the export's "Total assets"), so the AI saw an unreconcilable gap (e.g. $68k total vs $8k listed; $60k of holdings invisible). Same class as ISS-36/47/50. Fix: Investments section now reads `marketInvestments` (ticker/name/value/cat/shares/cost-basis/gain) + `savedPortfolio`; dead `physicalAssets` branch dropped (customAssets shown with `cat`). **Pure read-only export — not the save path** → autonomous-safe push. node harness `_aiexport_harness.mjs`: current omits $60k MI → fixed itemizes both, listed assets reconcile to `totalA` ($68k). Found in the item-1 `utils/aiExport.js` correctness scan. | v0.83.21 (2026-06-26) |
| ISS-50 | report / money / ratios | 🟢 | **Recurrence of ISS-46 + ISS-47 in the Summary report tab** (`SummarySection`, `clientReports.jsx:52` — the last report surface still on the non-canonical basis). (1) `mnd` (`:60`) summed **raw `Σ c.min`** instead of canonical `Σ effectiveMin(c)` → the Summary's **DSR**, **cash flow**, **Min Pay KPI**, income pie, and DSR recommendation diverged for no-min / paid-off-stale-min / below-$25-floor cards (contradicted the Financial-Statements DSR fixed in ISS-46). (2) `totA` (`:72`, Health-Score radar D/A axis) omitted **`marketInvestments`** (ISS-47 class) → DTA overstated for MI-holding clients. Both pure display derivations, **not** the save path (NMModal already uses `sumMin`; the raw-min save path is the separate ⛔ ISS-48). Fix: `effectiveMin` + add MI sum. node harness `iss50.mjs`: `mnd 160→125`, `totA 491k→611k`. | v0.83.20 (2026-06-26) |
| ISS-47 | report / money | 🟢 | `RatioContent` (`clientReports.jsx:189`) and the **Balance Sheet** of `FinancialStatementsTab` (`:190-191`) computed `totalA` as accounts + customAssets only — `marketInvestments` omitted (canonical §3 `totalA` includes it; `AssetsLiabilitiesTab` `:566` already did). Total-Assets KPI, Net Worth, and DTA were **understated** for any client holding market investments, and the holdings never appeared as a balance-sheet line. Fix: both `tA` derivations now add `Σ marketInvestments.value` (view-scaled like `customAssets`); MI holdings render as `📈 {ticker} {name}` rows inside the existing **Investment Assets** section with the **Total Investment** subtotal reconciled to `invA+miA` (no new UI string — folded into the existing section). Pure display, not save path. node harness: both `360k→393k`=canonical, DTA `56.94%→52.16%`, both surfaces now agree. | v0.83.19 (2026-06-26) |
| ISS-46 | report / ratios / money | 🟢 | `RatioContent` (`clientReports.jsx:189`, also Full Report `:311`) computed the DSR numerator from raw `Σ card.min` instead of canonical `Σ effectiveMin(card)` (§3 `sumMin`). Diverged for no-min cards (understated), paid-off cards w/ stale min (overstated), and below-$25-floor mins; same `minD` also fed the RSR available-cash base. Contradicted the Cash Flow Statement's DSR (`:144`, uses `sumMin`) on the same tab — ISS-45 class. Fix: `minD=cards.reduce((s,c)=>s+effectiveMin(c),0)`. Pure display, not save path. node harness 4 cases. | v0.83.18 (2026-06-26) |
| ISS-45 | calc / ratios / display | 🟢 | `ratFmt("dsr")` rendered the debt-service ratio as a **`×` multiple** (`0.36x`) because `dsr` was grouped with `currentRatio` in the formatter, instead of a **percentage** (`36%`). Visible in `RatioContent` (Financial Statements → Ratios) where the DSR card showed `0.36x` next to its own `< 36%` benchmark — contradicting every other DSR surface (RadialGauge/reminder both `*100+"%"`) and the canonical model (`golden-anchor-logic §3`). Fix: split the branch — `currentRatio` keeps `×` (unchanged), `dsr` → `(v*100).toFixed(0)+"%"` with `v>=1→"99+%"` for the no-income sentinel. Also removed the dead `const abs` in that branch (−1 `no-unused-vars`). Pure display derivation, not the save path. node harness: dsr→%, currentRatio proven unchanged. | v0.83.17 (2026-06-26) |
| ISS-44 | reminders / dates | 🟢 | `getClientRem` days-until-due used a **hardcoded 31** for the bill month-wrap (over-counts in 28/30-day months → bills due within 7 *real* days got hidden by the `diff<=7` filter near month-end) and `Math.max(0,dueDay-day)` for cards (no wrap → past-due-this-month cards showed `0`, sorting to the top). New `_daysUntilDue(dueDay,today)` helper computes the exact delta with real month lengths + day clamp + Dec→Jan rollover; both paths call it. Read-only display derivation (not the save path); keys/dismissals embed `YYYY-MM`, unaffected. node harness 8/8 edge cases. | v0.83.16 (2026-06-26) |
| ISS-43 | i18n (D-3) / fmt | 🟢 | `fmtS` (abbreviated `$5K`/`$1.2M`, 13 call sites) hardcoded `"$"` while `fmt` honors the user's currency (`_GA_CCY`) → non-USD picks (EUR/GBP/MXN/CAD) showed `$` on abbreviated values, `€`/`£`/`MX$` elsewhere. Now derives the symbol from the same Intl config `fmt` uses (`_ccySym`), K/M rounding unchanged. Found in the item-1 `utils/finance.js` helper scan (EN/ES symmetry audit clean 1874/1874). | v0.83.15 (2026-06-26) |
| ISS-41 | a11y (WCAG 4.1.2) | 🟢 | 3 icon-only `×` buttons had no accessible name (shared `Modal` close → all 44 modals; `IAdd` cancel; Intake detail close). Added bilingual `aria-label`+`title` via a `gaLabel` helper (reads `<html lang>`, reuses `close`/`cancel` keys). **Footgun: shared primitives don't receive `t` — resolve labels from `document.documentElement.lang`, kept in sync since v0.83.12.** | v0.83.14 (2026-06-26) |
| ISS-27 | api / scale | 🟢 | `admin-members` grant/revoke `patchByEmail` paged only first 200 auth users → silent no-op past #200; now paginates like `loadClients` | v0.83.11 (2026-06-26) |
| ISS-30–33 | i18n (D-3) | 🟢 | Calculator hardcoded-English (amort/equity table headers + clientCalcs prefill helpers + household block) → bilingual; 14 new keys EN+ES, 4 reused | v0.83.10 (2026-06-26) |
| ISS-29 | review / calc | 🟢 | `HomeEquityCalc` Interest Saved fabricated approx → now real (baseInt − extraInt) from shared amort loop | v0.83.9 (2026-06-26) |
| ISS-28 | review / calc | 🟢 | `HomeEquityCalc` Months Saved year-rounded vs exact → understated/0/neg; now exact-month difference | v0.83.9 (2026-06-26) |
| ISS-36 | review / money | 🟢 | aiExport card "min" called `payM(cd)` (wrong fn+arity) → always $0; now `effectiveMin(cd)` | v0.83.8 (2026-06-26) |
| ISS-35 | review / calc | 🟢 | `SavingsCalc` 0% APY divided by rate → NaN→$0; now guarded to simple sum | v0.83.8 (2026-06-26) |
| ISS-39 | review / calc-nav | 🟢 | `CalculatorsPage` synced `active`←`activeCalc` in a `useEffect` (synchronous setState → cascading render); on browser back/forward the prior view flashed one frame. Now derived during render (React "adjust state on prop change") — no flash, no effect. −2 lint errors. **Verified** deep-link + tile + back/forward on dev preview. | v0.83.13 (2026-06-26) |
| ISS-34 | review / nav | 🟢 | advisor Back/Forward compared string summary-id `===` number selectedId; now `String()===String()` | v0.83.8 (2026-06-26) |
| ISS-11 | lint / theme | 🟢 | `no-misleading-character-class` ×3 in `stripLeadEmoji` (invisible FE0F/ZWJ/20E3 in source) | 2026-06-26 — rewrote literal combining chars as `\u{…}` escapes + justified disable; behavior identical. **Footgun pattern: never leave raw combining/ZWJ code points in source — they resist byte patching; use `\u{…}` escapes.** |
| ISS-10 | security / deps | 🟢 | `npm audit` 4 vulns (form-data/ws/js-yaml/@babel) → 0 | 2026-06-26 — `npm audit fix` (lock-only; puppeteer-core unchanged, ws patch 8.20.1→8.21.0). Owner optional: PDF spot-check on prod. |
| ISS-02 | extraction / imports | 🟢 | Missing imports after Phase-2 carve-out → `ReferenceError` (admin.jsx `expBackup`; import.js `MS`; chartEditors `dashChartOptions`) | v0.83.3 / v0.83.6 / v0.83.7 — **recurrence pattern: watch every extraction for lost imports.** |
| ISS-01 | save path | 🟢 | Advisor save-success toast fired unconditionally — a failed save showed "✓ saved" (silent data-loss risk) | v0.83.1 (shipped to main 2026-06-25, owner-approved in test-mode) |

> **Recurrence pattern to watch (from ISS-02):** Phase-2/3 file extractions repeatedly drop a
> module-level import that was implicit in the old single-file scope. Any future extraction MUST
> re-run `npm run lint` and confirm `no-undef` is **0** before commit.
